# 장애 복구 결함 대장 (T93~)

> **서버나 인프라가 멎었다 돌아오는 경로의 결함**만 여기 둔다. 전수검사와 그
> 후속은 [`tickets-audit.md`](./tickets-audit.md)에 있다. 번호는 그쪽과 이어서
> 쓴다.
>
> 지금 코드의 규칙은 `domain.md`의 「서버가 죽어도 대회는 계속된다」, 판단 근거는
> 각 PR의 본문과 커밋이다. 여기는 **무엇이 깨졌고 무엇으로 닫았나**만 적는다.
> 상태 열이 진행 현황이다(`대기` → `완료 (#PR)`).

| # | 티켓 | 무엇이 깨지나 | 등급 | 상태 |
|---|---|---|---|---|
| T93 | 소켓이 끊기면 단말이 스스로 돌아오지 않는다 | 재시작하면 태블릿 전부가 에러 문구에서 멈춘다 | 높음 | **완료 (#114)** |
| T94 | 재시작이 차례였던 사람을 자동 폴드시킨다 | 정지 동안 흐른 턴 타이머가 부팅 직후 발화한다 | 치명 | **완료 (#113)** |
| T95 | 정지와 재개가 화면에 없다 | 서버가 멈췄다 돌아온 것을 아무 단말도 말하지 않는다 | 높음 | **완료 (#115)** |
| T96 | 소켓이 없는 화면은 정지를 모른다 | 참가자 폰과 전광판이 멈춘 대회를 정상 진행처럼 보여준다 | 중간 | **완료 (#116 · #117)** |
| T97 | Redis만 죽으면 차례였던 사람이 자동 폴드된다 | 장애 중 누른 버튼은 실패하고, 복구 뒤 시간 초과로 접힌다 | 치명 | **완료 (#120)** |
| T98 | Redis만 죽은 시간이 블라인드 시간으로 샌다 | 하트비트가 잡은 공백을 아무도 읽지 않는다 | 중간 | **완료 (#120)** |
| T99 | Redis가 죽어도 아무 화면도 모른다 | 누른 사람만 인프라 원문 에러를 7초 뒤에 본다 | 중간 | **완료 (#120)** |
| T100 | 리바인 창에 Redis 장애가 끼면 돈만 빠지거나 장애 때문에 탈락한다 | 수락하면 포인트는 빠지고 칩은 0, 안 누르면 서버 타이머가 거절로 센다 | 치명 | **완료 (#121)** |

---

## 완료

### T93 — 소켓이 끊기면 단말이 스스로 돌아오지 않는다

`onclose`가 에러 문구만 세우고 다시 붙지 않았다. **#114**가 좌석·딜러의 배선을
`useTableSocket` 하나로 모으고, 재접속 규칙을 `reconnect-policy.ts`로 뺐다 —
`Retry-After` 바닥 + 지터, 딜러는 좌석보다 늦게 붙는다.

**왜 못 잡았나**: 끊었다 다시 붙이는 시나리오가 e2e에도 부하 무대에도 없었다.

### T94 — 재시작이 차례였던 사람을 자동 폴드시킨다

블라인드 시계는 정지만큼 밀었는데 턴 시계는 아무도 안 밀었다. 폴드 경로가
둘이었다 — 큐에 남은 타임아웃 잡, 절대 시각인 `actionDeadline`. **#113**이
부팅에서 세대를 올려 잡을 무효로 만들고 마감을 다시 찍었다. 유예 60초는 T95가
걷어냈다.

**왜 못 잡았나**: 복구 스펙이 스냅샷과 블라인드만 봤다. 큐의 잡을 부팅 뒤에
돌려 보는 자리가 없었다.

### T95 — 정지와 재개가 화면에 없다

**#115**가 복구를 「멈춰 세우기」로 바꿨다 — `actionDeadline` 삭제, 세대 올림,
`TableState.resumePending`. 다시 여는 것은 딜러가 누르는 `DealerService.resumeTable`이고,
`resumePending`인 테이블은 좌석 액션을 거절한다.

### T96 — 소켓이 없는 화면은 정지를 모른다

REST로 읽는 폰·전광판이 `resumePending`을 못 봤고, 블라인드는 부팅 순간부터
흘렀다. **#116**이 좀비 소켓을 치우고(ping/pong · `keepalive` 침묵), **#117**이
`SYNCING`을 되살려 딜러 n/n까지 블라인드를 `pausedAt`에서 멈췄다. 설계는
[스펙](./superpowers/specs/2026-09-11-t96-syncing-design.md).

**왜 못 잡았나**: 부팅 보정을 단언하던 복구 검사 일곱이 **보정이 부팅에 있다는
것 자체**를 사실로 삼고 있었다.

### T97 · T98 · T99 — Redis만 죽는 경로

**T93~T96은 전부 부팅이 시작점이었다.** 백엔드가 살아 있고 Redis만 죽었다
돌아오면 부팅이 없어서, 그 넷이 닫은 것이 하나도 돌지 않았다. 2026-09-13
실측(`docker kill` → 30초 → `start`)에서 장애 중 누른 콜이 7초 뒤 ioredis 원문으로
거절되고(T99), 복구 17초 뒤 타임아웃 잡이 **이미 지난 마감**으로 그 사람을
폴드시켰고(T97), 대회는 `ONGOING` · `pausedMs=0`이었다(T98 — 하트비트 공백을
읽는 자리가 다음 부팅뿐이었다).

**#120**이 Redis 장애를 「짧은 부팅」으로 다룬다 — 끊기면 즉시 거절 · `SYNCING` ·
전원 방송, 돌아오면 부팅과 같은 함수로 락 안에서 멈춰 세우고 딜러가 재개한다.
설계는 [스펙](./superpowers/specs/2026-09-13-t97-redis-outage-design.md), 규칙은
`domain.md`의 「Redis만 죽었다 돌아와도 같은 길을 탄다」.

`npm run test:outage`가 실측과 같은 kill을 자동으로 돈다 — 폴드 0, 거절 1ms 안,
`pausedMs` 약 32초, 칩 총량 불변.

**왜 못 잡았나**: 복구 테스트가 전부 `recoverAll()`을 직접 불렀다. Redis만 죽이는
무대가 없었다. 그리고 최종 리뷰가 **복구 중 n/n 재집계가 스윕보다 먼저 대회를
여는 길**을 찾았다 — 부품(스윕 · 재집계)이 각각 옳은데 순서가 틀리면 같은 폴드가
난다.

### T100 — 리바인 창에 Redis 장애가 끼면 돈만 빠지거나 장애 때문에 탈락한다

T97 최종 리뷰가 잔여로 남긴 「장애 중 리바인 창」을 따라가 보니 **탈락보다 돈이 먼저였다.**
`executeRebuyTransaction`이 DB를 먼저 커밋하고 Redis에서 실패해 `processRebuy`가 0을
돌려줬고, 복구 뒤 체크포인트가 참가 행 스택을 스냅샷의 0으로 덮었다 — 포인트는 빠졌고
칩은 0이고 탈락도 안 된 좌석. 아무도 안 누르면 프로세스 타이머가 장애와 무관하게 흘러
거절로 셌다(코드 추적, 실측 아님).

**#121**이 쓰는 순서를 스냅샷 → DB → 전광판으로 바꾸고(장애가 돈이 움직이기 전에 멈춘다),
장애가 낀 리바인은 아무것도 결정하지 않고 딜러가 판을 다시 열 때 중단된 사람에게만 새로
묻는다. 설계는 [스펙](./superpowers/specs/2026-09-14-t100-rebuy-outage-design.md), 순서가
안전한지와 긴 대기가 끌고 오는 문제의 검수는 [계획서](./superpowers/plans/2026-09-14-t100-rebuy-outage.md)의
「검수」 절.

**왜 못 잡았나**: T97은 턴 경로만 봤다. 리바인은 락 밖의 사람 입력이라 T97의 가드
둘(게이트웨이 · `handleAction` 락 안) 어느 쪽도 지나지 않았다.

---

## 잔여 목록

| 자리 | 무엇 | 상태 |
|---|---|---|
| 실제 kill 무대 — 백엔드 | Redis kill은 `npm run test:outage`가 닫았다(T97). **백엔드 프로세스**를 진짜로 죽이는 자동 검사는 아직 없다 — 부팅 배선(`onApplicationBootstrap` → `recoverAll`)과 진짜 마지막 하트비트는 어느 테스트도 안 지난다 | 대기 |
| 조용한 끊김 | 망이 RST 없이 끊기면 감지가 TCP 타임아웃만큼 늦다. 그동안은 T97 이전과 같다. Redis 하트비트 공백 판정이 필요하다 | 대기 |
| Redis 데이터 유실 | 볼륨 없음 · AOF 손상 뒤의 런타임 재구성이 없다. `pauseTable`은 스냅샷 없는 테이블에 경고만 남기고, 재구성(`rebuildTable`)은 부팅 전용이다 | 대기 |
| 짧은 장애의 오프라인 큐 | **7초보다 짧은 장애**에서 ioredis가 들고 있던 명령을 뒤늦게 실행한다. 턴 쓰기는 `handleAction`의 장애 세대가 막지만, 턴 밖의 쓰기(착석 · 결제)는 보지 않았다 | 대기 (측정) |
| 리바인 되돌리기의 이중 장애 | DB가 리바인을 거절한 직후, 칩을 되돌리는 순간 Redis까지 끊기면 칩이 돈 없이 남는다(T100). `error` 로그뿐이다 | 감수 |
| 리바인 대기 중 프로세스 재시작 | 장애로 끊긴 리바인의 고리와 재개 대기가 메모리라 재시작하면 사라지고, 테이블이 `HAND_END`에 남는다. T100 전에도 15초 창 안의 재시작은 같았다 | 대기 |
| 재접속한 좌석의 리바인 프롬프트 | 끊겼다 다시 붙은 좌석은 이미 뜬 `REBUY_PROMPT`를 다시 받지 못한다(이벤트라서). 원래 있던 결함이다 | 대기 |
| 딜러가 끝내 재개하지 않는 리바인 | 장애로 끊긴 리바인은 딜러의 「이어서 진행」을 기다리므로, 안 누르면 그 테이블의 정산이 멈춘다. 정지 배너가 서 있어 딜러가 안다 | 감수 (T100) |
| 재개 대기 중 대회 중단 | 긴 장애 뒤 상점이 「이어서 진행」 대신 중단을 누르면 스냅샷이 지워져 재개가 영영 안 된다. `DealerService.askRebuys`의 대기 · `rebuyInFlight` · 걸린 `RESOLVE_WINNERS` 요청이 프로세스 재시작까지 남는다(테이블당 하나, 돈 무관). `TOURNAMENT_CLOSED`에서 대기를 풀면 닫힌다 | 대기 |
| 리바인 칩 쓰기와 DB 사이의 프로세스 크래시 | T100이 스냅샷을 먼저 쓰므로, 그 사이에 프로세스가 죽으면 칩은 들어가고 돈은 안 빠진다(상점 손해 방향). 이전 순서에서는 반대로 참가자가 돈을 잃었다 | 감수 (T100) |
| 겹친 스윕의 재정지 | 스윕 중 다시 끊겨 스윕이 둘 돌 때, 늦은 스윕이 딜러가 방금 재개한 테이블을 다시 멈출 수 있다. 폴드·칩 이동은 없고 재정지가 방송되지 않아 딜러 화면이 새로고침 전까지 재개 버튼을 안 보인다. `pauseTable`에 장애 세대를 넘겨 막을 수 있다 | 감수 (드묾) |
| `test:outage`의 빨간불 여유 | 사후 확인(스윕의 `freezeTournament`를 건너뛴다)의 폴드가 복귀 약 16초 뒤에 나서 40초 관찰 창 안의 여유가 약 2.4배다. BullMQ 지연이 늘면 빨간불이 조용히 초록이 된다(초록 쪽은 영향 없음) | 감수 |
| `WsGateway.sweepSockets`의 틱 비용 | 10초마다 모든 소켓에 `ping()`과 `keepalive`를 동기로 보낸다. 12,000명이면 틱마다 쓰기 약 2.4만 번이라 `my_action_server_ms`에 10초 주기 스파이크로 보일 수 있다. 다음 램프에서 본다 | 대기 (측정) |
| `WsGateway.recount`의 k 표시 | `broadcast`가 OPEN이 아닌 소켓을 방에서 먼저 지우면 뒤이은 `handleDisconnect`의 재집계가 안 돈다. n/n 판정은 OPEN 딜러만 세어 안전하고, 「k/n 복귀」 표시만 다음 이벤트까지 높게 남는다 | 감수 (T96) |
