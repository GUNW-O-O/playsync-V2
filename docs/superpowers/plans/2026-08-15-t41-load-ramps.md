# T41 램프 실행 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 성장형 부하 램프를 만들어 돌리고, 이 시스템이 동시접속을 어디까지 받고 몰림을 어디까지 견디는지 숫자로 남긴다.

**Architecture:** k6 `ramping-vus`로 VU(= 테이블)를 등차로 늘리되 아무도 내리지 않는다. 새 VU는 상점 콘솔 경로로 테이블을 열고 좌석 아홉을 제품 경로로 채워 합류하고, 이미 도는 테이블은 무한 리바인으로 인원을 유지한 채 계속 핸드를 돈다. 구간을 `grow-N`(몰림)과 `steady-N`(정상 운영)으로 태그해 피크 내성과 동시접속 상한을 갈라 읽는다. 사용자 불편선(내 액션 p95 1초)이나 서버 붕괴선(lag p95 100ms)에 연속 두 번 닿으면 `test.abort()`로 멈춘다.

**Tech Stack:** k6 (`k6/experimental/websockets`, `k6/execution`, `k6/timers`), NestJS + Prisma + Redis (피측정자), Docker Compose (`backend/docker-compose.test.yml`의 `load`·`k6` 프로파일)

## Global Constraints

설계 문서는 [`docs/superpowers/specs/2026-08-15-load-ramp-design.md`](../specs/2026-08-15-load-ramp-design.md)다. 아래는 그중 **모든 태스크에 걸리는 것**만 옮긴 것이고, 값은 그대로 쓴다.

- **닉네임은 3~10자다** (`CreateUserDto`). 램프에서 VU 번호가 커지면 접두사 길이가 상한을 넘는다.
- **WS 경로는 `/playsync`다.** 루트로 붙으면 핸드셰이크 이전에 거절돼 서버 로그에도 남지 않는다.
- **WS는 `Origin` 헤더가 필수다.** 값은 컨테이너의 `WS_ALLOWED_ORIGINS`(= `http://localhost:3000`)와 같아야 한다.
- **WS 티켓은 1회용이다.** 소켓마다 `POST /ws/ticket`을 새로 부른다.
- **메트릭 응답의 `resolutionMs`가 지연의 바닥값이다.** 유휴 서버도 p50이 약 10ms이므로 빼고 읽는다.
- **`teardown`과 `handleSummary` 사이에 모듈 상태가 이어지지 않는다.** 서버 지표는 모니터가 기록한 k6 지표에서 읽는다.
- **k6 컨테이너는 `../load`만 마운트한다.** 매니페스트는 `/load/.load-seed.json`이다.
- **Git Bash에서 k6를 직접 부를 때는 `MSYS_NO_PATHCONV=1`이 필요하다.** npm 스크립트는 cmd를 타므로 무관하다.
- **부하 실행과 `npm run test:int`를 동시에 돌리지 않는다.** 저장소를 나눠 쓰므로 서로 데이터를 지운다.
- **커밋 메시지·코드 주석·문서는 한국어다.** PR 제목과 본문도 한국어다.
- 임계값 기본: `LOAD_LAG_WARN_MS=20`, `LOAD_LAG_BREACH_MS=100`, 사용자 불편선 `LOAD_MY_ACTION_ABORT_MS=1000`, 연속 판정 횟수 2.
- 계정 풀은 시드 기본값 **600**을 그대로 쓴다. 66테이블(594명)이 상한이다.

---

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `backend/src/store/session/session.service.ts` | 대회 생성 시 빈 스냅샷을 함께 세운다 (T38 불변식) | 1 |
| `backend/src/store/session/session.service.int-spec.ts` | 위의 회귀 테스트 | 1 |
| `backend/prisma/seed-load.ts` | 상점·대회를 `LOAD_STORES`개 세우고 매니페스트를 배열로 낸다 | 2 |
| `load/lib/table.js` | 액션 믹스, 딜러 승자 선택, 리바인 수락, 롤링 창 중단 판정 | 3 |
| `load/lib/api.js` | 테이블 생성 재시도(409) | 4 |
| `load/lib/monitor.js` | 연속 붕괴 판정과 `test.abort()`, 단계 라벨 | 4 |
| `load/scenarios/ramp.js` | 성장 램프. `LOAD_TABLES_PER_STORE`로 A·B가 갈린다 | 4 |
| `load/lib/summary.js` | 램프 요약(단계별 · 중단 사유) | 4 |
| `load/scenarios/smoke.js` | 매니페스트 모양 변경에 맞춘다 | 2 |
| `package.json` | `load:ramp-a` · `load:ramp-b` 스크립트 | 4 |
| `docs/results/2026-08-15-load-ramp.md` | 곡선과 결론 | 6 |
| `docs/tickets-next.md`, `docs/backlog.md`, `load/README.md`, `CLAUDE.md` | 기록 갱신 | 6 |

---

### Task 1: 대회 생성이 빈 스냅샷을 함께 세운다

T38이 세운 불변식은 "테이블이 있으면 스냅샷이 있다"이고, 그래야 "스냅샷이 없다"의 뜻이 **유실 하나로** 좁아진다. `createTable`은 그것을 지키는데(`session.service.ts:220,235`) `createSession`은 좌석 비트맵만 세운다(`:181`). 즉 불변식이 지금 **경로 하나에서만 참**이다.

증상은 딜러가 먼저 붙는 실제 순서에서 나온다 — `GET /playsync/:tableId`가 500이고(`playsync.service.ts:41`), WS는 `data: null`을 조용히 보내 화면이 빈 펠트로 선다. 착석이 `createEmptyTableState`로 덮어 주므로 손님이 먼저 앉으면 드러나지 않는다.

**Files:**
- Modify: `backend/src/store/session/session.service.ts:181`
- Test: `backend/src/store/session/session.service.int-spec.ts` (기존 `createSession — OTP 해시 통합` describe 안에 추가)

**Interfaces:**
- Consumes: `RedisService.saveSnapShot(tableId, state)`, `createEmptyTableState(tournamentId)` — 둘 다 `session.service.ts`가 이미 import하고 있다(`:15`, `:235`에서 사용).
- Produces: 없음. 이 태스크는 제품 동작만 바꾼다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`session.service.int-spec.ts`의 `describe('SessionService.createSession — OTP 해시 통합', ...)` 블록 **안**, `it('대회 조회 응답에는 OTP도 해시도 실리지 않는다', ...)` 바로 뒤에 넣는다. `GamePhase`는 파일 상단(`:15`)에서 이미 import돼 있다.

```ts
  /**
   * 대회를 만들면 1번 테이블의 빈 스냅샷도 함께 선다.
   *
   * T38이 "테이블이 있으면 스냅샷이 있다"를 세웠고, 그래야 "스냅샷이 없다"의
   * 뜻이 유실 하나로 좁아진다. `createTable`은 그것을 지키는데 이 경로는
   * 좌석 비트맵만 세우고 있었다 — 불변식이 경로 하나에서만 참이었다.
   *
   * 드러나는 순서는 딜러가 먼저 붙을 때다. 손님이 먼저 앉으면 `enterSeat`이
   * `createEmptyTableState`로 덮어 주므로 보이지 않는다.
   */
  it('대회를 만들면 1번 테이블의 빈 스냅샷도 함께 선다', async () => {
    const created = await sessionService.createSession(makeCreateDto());
    const tableId = created.tables[0].id;

    const raw = await redis.get(`table:state:${tableId}`);
    expect(`스냅샷 ${raw === null ? '없음' : '있음'}`).toBe('스냅샷 있음');

    const state = JSON.parse(raw!);
    expect(`phase ${state.phase}`).toBe(`phase ${GamePhase.WAITING}`);
    expect(`좌석 수 ${state.players.length}`).toBe('좌석 수 9');
    expect(`착석자 ${state.players.filter((p: unknown) => p !== null).length}`)
      .toBe('착석자 0');
    expect(`대회 ${state.tournamentId}`).toBe(`대회 ${created.id}`);
  });
```

- [ ] **Step 2: 돌려서 실패를 본다**

```bash
cd backend && KEEP_TEST_CONTAINERS=1 npx jest --config ./test/jest-int.json \
  --testPathPatterns session.service.int-spec -t '빈 스냅샷도 함께 선다'
```

컨테이너가 안 떠 있으면 먼저 `npm run test:int:up -w backend`.

Expected: FAIL. `Expected: "스냅샷 있음"` / `Received: "스냅샷 없음"`.

**실패 메시지를 그대로 보고서에 옮긴다.** 통과부터 하면 가짜 RED다.

- [ ] **Step 3: 제품 코드를 고친다**

`session.service.ts:181`. 지금:

```ts
    if (!sessionInfo) throw new InternalServerErrorException('세션을 만들지 못했습니다.');
    await this.redis.setSeatBitmap(sessionInfo.id, sessionInfo.tables[0].id);
```

바꾼 뒤:

```ts
    if (!sessionInfo) throw new InternalServerErrorException('세션을 만들지 못했습니다.');
    await this.redis.setSeatBitmap(sessionInfo.id, sessionInfo.tables[0].id);
    // **스냅샷의 수명을 테이블의 수명에 맞춘다.** T38이 `createTable`에서
    // 세운 불변식("테이블이 있으면 스냅샷이 있다")은 대회 생성 경로에도
    // 걸려야 한다. 아니면 "스냅샷이 없다"가 유실과 정상 둘 다를 뜻하게 되고,
    // 딜러가 손님보다 먼저 붙는 실제 순서에서 `GET /playsync/:tableId`가
    // 500을 낸다.
    await this.redis.saveSnapShot(
      sessionInfo.tables[0].id,
      createEmptyTableState(sessionInfo.id),
    );
```

- [ ] **Step 4: 돌려서 통과를 본다**

```bash
cd backend && KEEP_TEST_CONTAINERS=1 npx jest --config ./test/jest-int.json \
  --testPathPatterns session.service.int-spec
```

Expected: 이 파일의 스위트 전부 PASS. 새 케이스 포함 개수를 보고서에 적는다.

- [ ] **Step 5: 커밋**

```bash
git add backend/src/store/session/session.service.ts backend/src/store/session/session.service.int-spec.ts
git commit -m "fix: 대회를 만들 때도 빈 스냅샷을 세운다"
```

---

### Task 2: 시드가 상점과 대회를 여럿 세운다

램프 A는 6테이블마다 다음 대회로 넘어간다. 그 대회들을 **실행 중에 만들 수 없다** — k6에 VU 간 공유가 없어 7번째 VU가 앞선 VU가 만든 대회의 id를 알 방법이 없다. 대회 생성은 실제로도 대회 몇 시간 전에 한 번 일어나는 일이라 측정 밖이 맞다(T40이 bcrypt `hash`/`compare`를 시점으로 가른 것과 같은 판단).

**Files:**
- Modify: `backend/prisma/seed-load.ts`
- Modify: `load/scenarios/smoke.js` (매니페스트 모양이 바뀐다)

**Interfaces:**
- Produces: `/load/.load-seed.json`의 새 모양. 태스크 3·4가 이것을 읽는다.

```jsonc
{
  "password": "loadtest1234",
  "ownerNickname": "loadowner",
  "dealerOtp": "123456",
  "startStack": 100000,
  "bigBlind": 200,            // 봇의 레이즈 단위. sb × 2
  "accountPrefix": "p",
  "accountPool": 600,
  "tournaments": [            // 길이 = LOAD_STORES
    { "id": "…", "storeId": "…", "tables": [{ "id": "…", "tableOrder": 1 }] }
  ]
}
```

- [ ] **Step 1: 규모 상수와 주석을 넣는다**

`seed-load.ts`의 `INITIAL_TABLES` 선언 바로 위에 추가한다.

```ts
/**
 * 세울 상점(= 대회) 수. **램프 A의 x축이다.**
 *
 * 램프가 실행 중에 대회를 만들 수 없어서 여기서 세운다 — k6는 VU 사이에
 * 상태를 공유하지 않으므로(`SharedArray`는 읽기 전용, `setup()`은 실행 전에
 * 한 번) 7번째 VU가 앞선 VU가 만든 대회의 id를 알 방법이 없다.
 *
 * 측정 밖에 두는 것이 옳기도 하다. 대회 생성은 실제로 대회 몇 시간~며칠 전에
 * 상점이 한 번 하는 일이고, 대회 직전에 몰리는 것은 로그인과 착석이다 —
 * T40이 bcrypt의 `hash`와 `compare`를 시점으로 가른 것과 같은 자리다.
 *
 * 기본 12인 이유는 계정 풀 600이 66테이블을 덮고, 램프 A(상점당 6테이블)로
 * 환산하면 11상점이기 때문이다.
 */
const STORE_COUNT = Number(process.env.LOAD_STORES ?? 12);
```

- [ ] **Step 2: 무대 세우는 부분을 상점 단위 루프로 바꾼다**

`const owner = await prisma.user.create(...)`부터 테이블 루프 끝(`manifest` 선언 직전)까지를 통째로 대체한다. 계정 풀 생성은 루프 **밖**에 그대로 둔다 — 대회를 가리지 않는 공용 풀이다.

```ts
    const owner = await prisma.user.create({
      data: { nickname: OWNER_NICKNAME, password, role: Role.STORE_ADMIN },
    });

    // 풀 계정. `createMany`로 한 번에 넣는다 — 해시가 전부 같은 값이라
    // bcrypt는 위에서 이미 한 번만 돌았다. 대회를 가리지 않는 공용 풀이다.
    if (ACCOUNT_POOL > 0) {
      await prisma.user.createMany({
        data: Array.from({ length: ACCOUNT_POOL }, (_, i) => ({
          nickname: `${ACCOUNT_PREFIX}${String(i).padStart(4, '0')}`,
          password,
          role: Role.USER,
        })),
      });
    }

    // 상점 하나 = 대회 하나. 브로드캐스트가 대회 경계를 넘지 않으므로
    // (`ws.gateway.ts`의 Map이 tournamentId 키다) 부하의 단위도 대회다.
    // 상점을 따로 만드는 것은 "홀덤펍 몇 곳"이라는 질문의 모양을 지키기
    // 위해서고, 소유자는 하나라 램프가 토큰을 하나만 들면 된다.
    const tournaments: {
      id: string;
      storeId: string;
      tables: { id: string; tableOrder: number }[];
    }[] = [];

    for (let s = 0; s < STORE_COUNT; s++) {
      const store = await prisma.store.create({
        data: { name: `${STORE_NAME} ${s + 1}`, ownerId: owner.id },
      });

      const blind = await prisma.blindStructure.create({
        data: { name: '부하 (한 레벨)', structure: BLIND_STRUCTURE, storeId: store.id },
      });

      const tournament = await prisma.tournament.create({
        data: {
          name: `${TOURNAMENT_NAME} ${s + 1}`,
          status: TournamentStatus.PENDING,
          storeId: store.id,
          blindId: blind.id,
          // 위 주석 참고 — 포인트 충전 경로가 없어서 0이어야 결제가 통과한다.
          entryFee: 0,
          startStack: START_STACK,
          rebuyUntil: REBUY_UNTIL,
          itmCount: 1,
          prizePayouts: [{ place: 1, percent: 100 }],
          isRegistrationOpen: true,
          dealerOtpHash: await hashDealerOtp(DEALER_OTP),
        },
      });

      // `Table.dealerId`가 필수라 대회마다 딜러 세션이 하나 있어야 한다.
      const dealerSession = await prisma.dealerSession.create({
        data: { tournamentId: tournament.id },
      });

      const tables: { id: string; tableOrder: number }[] = [];
      for (let order = 1; order <= INITIAL_TABLES; order++) {
        const table = await prisma.table.create({
          data: { tableOrder: order, tournamentId: tournament.id, dealerId: dealerSession.id },
        });
        tables.push({ id: table.id, tableOrder: table.tableOrder });

        // 제품 경로에서는 `createTable`이 둘 다 한다. 시드는 그 경로를 타지
        // 않으므로 여기서 세운다 — 비트맵이 없으면 좌석 목록이 비어 있고,
        // 스냅샷이 없으면 딜러 화면이 500을 받는다(T38).
        await setSeatBitmap(redis, tournament.id, table.id);
        await setEmptySnapshot(redis, tournament.id, table.id);
      }

      tournaments.push({ id: tournament.id, storeId: store.id, tables });
    }
```

- [ ] **Step 3: 매니페스트와 로그를 바꾼다**

`const manifest = {...}`부터 `console.log` 묶음까지를 대체한다.

```ts
    const manifest = {
      ownerNickname: OWNER_NICKNAME,
      password: LOAD_PASSWORD,
      dealerOtp: DEALER_OTP,
      startStack: START_STACK,
      // 봇의 레이즈 단위. 블라인드 구조의 sb를 두 배 한 값이고, 레벨이
      // 하나뿐이라 실행 내내 고정이다.
      bigBlind: BLIND_STRUCTURE[0].sb * 2,
      accountPrefix: ACCOUNT_PREFIX,
      accountPool: ACCOUNT_POOL,
      tournaments,
    };
    mkdirSync(resolve(MANIFEST_PATH, '..'), { recursive: true });
    writeFileSync(MANIFEST_PATH, `${JSON.stringify(manifest, null, 2)}\n`, 'utf8');

    console.log('부하 시드 완료');
    console.log(`  상점·대회  ${tournaments.length}개`);
    console.log(`  테이블     대회마다 ${INITIAL_TABLES}개 (나머지는 램프가 연다)`);
    console.log(`  계정 풀    ${ACCOUNT_POOL}개 (${ACCOUNT_PREFIX}0000 ~)`);
    console.log(`  딜러 OTP   ${DEALER_OTP}`);
    console.log(`  매니페스트 ${MANIFEST_PATH}`);
```

- [ ] **Step 4: 스모크를 새 매니페스트에 맞춘다**

`load/scenarios/smoke.js`에서 `manifest.tournamentId`와 `manifest.tables[0]`를 쓰는 세 곳을 고친다. `export default function` 첫 줄과 `setup` 아래를 다음처럼 만든다.

```js
export function setup() {
  // 상점 관리자는 시드가 만들어 뒀다. 여기서 bcrypt 대조가 한 번 돈다.
  const ownerToken = login(manifest.ownerNickname, manifest.password);
  const runId = Math.random().toString(36).slice(2, 6);
  return { ownerToken, runId };
}

export default function (data) {
  // 스모크는 첫 대회의 첫 테이블만 쓴다. 시드가 대회를 여럿 세우는 것은
  // 램프 A를 위해서고(`LOAD_STORES`), 스모크의 관심사가 아니다.
  const tournament = manifest.tournaments[0];
  const table = tournament.tables[0];
```

이하 본문의 `manifest.tournamentId`를 전부 `tournament.id`로 바꾼다 (`seatPlayers`의 인자, `startTournament`의 인자, `runHands`의 인자 — 세 곳).

- [ ] **Step 5: 시드를 돌려 확인한다**

```bash
npm run load:up
npm run seed:load
```

Expected: `상점·대회  12개`가 찍히고, `load/.load-seed.json`의 `tournaments` 배열 길이가 12다.

```bash
node -e "const m=require('./load/.load-seed.json'); console.log(m.tournaments.length, m.bigBlind, m.tournaments[0].tables.length)"
```

Expected: `12 200 1`

- [ ] **Step 6: 스모크가 아직 도는지 본다**

```bash
npm run load:smoke
```

Expected: `smoke · 핸드 <0보다 큼> · … · 소켓오류 0`. 이 시점의 봇은 아직 콜만 하므로 T40 실측(핸드 130 안팎)과 비슷해야 한다.

- [ ] **Step 7: 커밋**

```bash
git add backend/prisma/seed-load.ts load/scenarios/smoke.js
git commit -m "feat: 부하 시드가 상점과 대회를 여럿 세운다"
```

---

### Task 3: 봇이 사람처럼 친다 — 액션 믹스 · 승자 · 리바인

지금 봇은 콜만 하고 딜러가 전원을 승자로 넣는다. 그래서 팟이 낸 만큼 되돌아오고 **탈락자가 한 명도 안 나온다** — `resolveWinners`의 리바인 분기(`dealer.service.ts:309`)가 한 번도 실행되지 않는다. 부하의 한 갈래(리바인 · 탈락 · 사이드팟)가 통째로 비어 있다.

**Files:**
- Modify: `load/lib/table.js`

**Interfaces:**
- Produces: 태스크 4가 쓰는 것들
  - `runHands({ tournamentId, tableId, dealerOtp, players, durationMs, stepLabel })` — `stepLabel`은 인자 없는 함수이고, 지표에 붙일 현재 단계 이름을 돌려준다. 없으면 태그를 붙이지 않는다.
  - 새 Counter export: `rebuysAccepted`, `raises`, `folds`
  - `myActionMs` · `othersActionMs` · `handsPlayed` · `socketErrors` · `absentActions` · `lateActions` · `signups` · `logins` 는 이름과 의미 그대로 남는다.

- [ ] **Step 1: 액션 믹스 상수와 지표를 넣는다**

`NEW_USER_RATIO` 선언 아래에 붙인다.

```js
/**
 * 합법 액션 중에서 어떻게 고르나. **부하의 모양을 정하는 두 번째 값이다.**
 *
 * 원래는 콜만 했다. 근거는 "레이즈를 섞으면 스택이 빨리 갈려 램프 중간에
 * 테이블이 빈다"였는데, 무한 리바인이 인원을 유지하면서 그 근거가 사라졌다.
 *
 * | | 왜 이 액션이 필요한가 |
 * |---|---|
 * | 체크·콜 | 기본. 핸드가 끝까지 가야 플랍·턴·리버·쇼다운이 전부 돈다 |
 * | 레이즈 | `resetChecked()`가 돌아 라운드가 한 바퀴 더 간다(`table-engine.ts:333`). 액션 수가 늘고 fan-out이 그만큼 곱해진다 |
 * | 폴드 | 없으면 사이드팟이 안 생기고 팟이 늘 전원 분배다 |
 */
const RAISE_RATIO = Number(__ENV.LOAD_RAISE_RATIO || 0.2);
const FOLD_RATIO = Number(__ENV.LOAD_FOLD_RATIO || 0.15);

/** 리바인을 수락한 횟수. 리바인 경로가 실제로 돌았다는 증거다. */
export const rebuysAccepted = new Counter('rebuys_accepted');
/** 레이즈와 폴드 횟수. 믹스가 의도대로 나왔는지 결과에서 본다. */
export const raises = new Counter('raises');
export const folds = new Counter('folds');
```

- [ ] **Step 2: 좌석 봇의 액션 선택을 쓴다**

같은 파일, `thinkMs()` 함수 바로 아래에 넣는다.

```js
/**
 * 이 상태에서 이 좌석이 낼 액션 하나.
 *
 * **합법인 것 중에서만 고른다.** 불법 액션은 엔진이 던지지만
 * (`table-engine.ts:55`의 "콜이 필요합니다"), 그 거절이 부하에 섞이면 재는
 * 것이 게임이 아니라 에러 경로가 된다.
 *
 * @param bigBlind 레이즈 단위. 시드 매니페스트의 값이다.
 */
function pickAction(state, me, bigBlind) {
  // 브로드캐스트 페이로드에 `currentBet`이 없으면(스키마가 바뀌면) 모든
  // 판정이 NaN이 되어 조용히 콜만 하게 된다. 없는 것을 0으로 접지 않고
  // 먼저 확인하는 이유는, 조용히 옛 동작으로 돌아가는 것이 가장 나쁜
  // 실패 모드이기 때문이다.
  if (typeof state.currentBet !== 'number') {
    throw new Error('renderGame에 currentBet이 없다 — 액션 믹스를 판정할 수 없다');
  }
  const toCall = state.currentBet - me.bet;
  const canCheck = toCall <= 0;
  // 레이즈는 `currentBet`보다 큰 목표 총액을 낼 수 있어야 성립한다.
  // 스택이 그에 못 미치면 레이즈를 빼고 콜(= 올인)로 접는다.
  const canRaise = me.stack + me.bet > state.currentBet;

  const r = Math.random();
  if (canRaise && r < RAISE_RATIO) {
    // **`amount`는 목표 총 베팅액이다.** 엔진이 보는 것은
    // `betAmount > currentBet` 하나뿐이고 최소 레이즈 규칙이 없다
    // (`table-engine.ts:321`). 스택을 넘으면 `executeBet`이 잘라 올인이
    // 되고, 그 자리가 사이드팟을 만든다 — 일부러 막지 않는다.
    const step = 1 + Math.floor(Math.random() * 3); // 1~3 BB
    const amount = state.currentBet + step * bigBlind;
    raises.add(1);
    return { action: 'RAISE', amount };
  }
  // 낼 것이 없는데 폴드하는 사람은 없다. 체크가 가능하면 체크다.
  if (!canCheck && r < RAISE_RATIO + FOLD_RATIO) {
    folds.add(1);
    return { action: 'FOLD' };
  }
  return canCheck ? { action: 'CHECK' } : { action: 'CALL' };
}
```

- [ ] **Step 3: `runHands`의 시그니처와 좌석 분기를 바꾼다**

`runHands`의 인자에 `bigBlind`와 `stepLabel`을 더하고, `step()`의 좌석 분기 마지막 줄(`send(entry, { event: 'PLAYER_ACTION', data: { action: 'CALL' } });`)을 대체한다.

```js
export function runHands({
  tournamentId,
  tableId,
  dealerOtp,
  players,
  durationMs,
  bigBlind,
  stepLabel,
}) {
```

좌석 분기:

```js
    // 합법 액션 중에서 고른다. 콜만 하면 팟이 늘 전원에게 되돌아와
    // 탈락이 없고, 그러면 리바인·사이드팟 경로가 통째로 안 돈다.
    send(entry, { event: 'PLAYER_ACTION', data: pickAction(state, me, bigBlind) });
```

- [ ] **Step 4: 딜러가 승자를 한 명만 고르게 바꾼다**

`step()`의 딜러 `SHOWDOWN` 분기를 대체한다.

```js
      if (state.phase === GamePhase.SHOWDOWN) {
        // **승자를 무작위 한 명으로 고른다.** 승자는 계산되는 값이 아니라
        // 딜러가 입력하는 값이므로(카드는 물리다) 봇이 카드를 못 봐도 이
        // 경로는 정당하다.
        //
        // 예전에는 폴드 안 한 전원을 한 그룹으로 넣었다(보드 하이). 그러면
        // 팟이 낸 만큼 되돌아와 **아무도 터지지 않고**, 리바인 분기
        // (`dealer.service.ts:309`)가 한 번도 실행되지 않는다.
        const alive = state.players.filter((p) => p && !p.hasFolded).map((p) => p.id);
        if (alive.length > 0) {
          const winner = alive[Math.floor(Math.random() * alive.length)];
          handsPlayed.add(1);
          send(entry, {
            event: 'DEALER_ACTION',
            data: { action: 'RESOLVE_WINNERS', winnerGroups: [[winner]] },
          });
        }
      }
```

- [ ] **Step 5: 리바인 팝업에 답한다**

`ws.onmessage` 안, `if (parsed.event !== 'renderGame' || !parsed.data) return;` **앞**에 넣는다. 리바인 팝업은 `renderGame`이 아니므로 그 가드 뒤에 두면 도달하지 않는다.

```js
      // 리바인 팝업(`ws.gateway.ts:316`)은 좌석 하나에게만 간다. 즉시
      // 수락해 좌석이 비지 않게 한다 — 램프의 규모 축이 인원 감소로
      // 흔들리면 안 된다.
      //
      // `processRebuy`는 이 응답을 락 **밖에서** 최대 15초 기다리고
      // (`REBUY_TIMEOUT_MS`), 그동안 `HAND_END`가 다음 핸드를 막는다.
      // 즉시 답해도 왕복이 끼므로 핸드 주기가 늘어난다 — 실제 대회도 그렇다.
      if (parsed.event === 'REBUY_PROMPT') {
        rebuysAccepted.add(1);
        entry.ws.send(JSON.stringify({ event: 'REBUY_RESPONSE', data: { accept: true } }));
        return;
      }
```

- [ ] **Step 6: 지표에 단계 태그를 붙인다**

`ws.onmessage`의 지연 기록 블록을 대체한다. `stepLabel`이 없으면 지금과 같이 동작한다.

```js
      // 지연 기록 — 보낸 시각과 받은 시각이 같은 VU 안에 있다.
      const win = windows.find((w) => !w.seen.has(idx));
      if (win) {
        win.seen.add(idx);
        const elapsed = Date.now() - win.at;
        // 단계 태그가 붙어야 원시 시계열에서 "테이블 12개 구간"을 갈라
        // 볼 수 있다. 램프가 아니면(스모크) 라벨이 없다.
        const tags = stepLabel ? { step: stepLabel() } : undefined;
        if (idx === win.actorSocketIdx) myActionMs.add(elapsed, tags);
        else othersActionMs.add(elapsed, tags);
        while (windows.length > 0 && windows[0].seen.size >= sockets.length) {
          windows.shift();
        }
      }
```

- [ ] **Step 7: 스모크에 `bigBlind`를 넘긴다**

`load/scenarios/smoke.js`의 `runHands` 호출에 한 줄 더한다.

```js
  return runHands({
    tournamentId: tournament.id,
    tableId: table.id,
    dealerOtp: manifest.dealerOtp,
    players,
    durationMs: DURATION_MS,
    bigBlind: manifest.bigBlind,
  });
```

- [ ] **Step 8: 스모크로 세 경로가 실제로 도는지 본다**

```bash
npm run seed:load && npm run load:smoke
```

스모크는 생각 시간이 0이라 핸드가 빨리 돈다. 확인할 것:

```bash
node -e "const s=require('./load/results/smoke-latest.json'); console.log(s.hands, s.socketErrors, s.raises, s.folds, s.rebuysAccepted)"
```

Expected: 핸드 > 0, 소켓오류 == 0, **`raises` > 0 · `folds` > 0 · `rebuysAccepted` > 0**.

`rebuysAccepted`가 0이면 탈락이 한 번도 안 났다는 뜻이다. 스택 10만에 SB 100이라 한 명이 팟을 다 가져가도 아홉 명이 마르는 데 여러 핸드가 걸린다 — 그럴 때는 `LOAD_DURATION_MS`를 늘려 다시 본다(`-e LOAD_DURATION_MS=120000`). 그래도 0이면 리바인 경로가 안 도는 것이므로 **거기서 멈추고 원인을 찾는다.**

- [ ] **Step 9: 커밋**

```bash
git add load/lib/table.js load/scenarios/smoke.js
git commit -m "feat: 봇이 액션을 섞고 딜러가 승자를 고른다"
```

---

### Task 4: 성장 램프

**Files:**
- Create: `load/scenarios/ramp.js`
- Modify: `load/lib/api.js` (테이블 생성 재시도)
- Modify: `load/lib/monitor.js` (연속 판정 · `test.abort()` · 단계 라벨)
- Modify: `load/lib/summary.js` (램프 요약)
- Modify: `package.json` (`load:ramp-a`, `load:ramp-b`)

**Interfaces:**
- Consumes: 태스크 2의 매니페스트(`tournaments` 배열, `bigBlind`), 태스크 3의 `runHands({..., bigBlind, stepLabel})`와 새 카운터들
- Produces: `load/results/ramp-a-latest.json`, `ramp-b-latest.json`, 그리고 `--out json=`이 남기는 원시 시계열

- [ ] **Step 1: 테이블 생성 재시도를 api.js에 넣는다**

`createTable` 아래에 붙인다.

```js
/**
 * 테이블을 열되 409를 재시도한다.
 *
 * `insertTable`이 트랜잭션 **안에서** `tableOrder` 최댓값을 뽑고 최종 방어가
 * `@@unique([tournamentId, tableOrder])`다(`session.service.ts:195-197`).
 * 램프에서는 여러 VU가 같은 순간에 같은 대회의 테이블을 열므로 진 쪽이
 * 409를 받는다. 상점 콘솔에서는 사람이 다시 누르면 되지만 봇은 스스로
 * 다시 눌러야 한다.
 *
 * **재시도 횟수 자체가 산출물이다.** 버튼 하나로는 보이지 않는 값이다.
 */
export function createTableWithRetry(ownerToken, tournamentId, onRetry) {
  for (let attempt = 0; attempt < 10; attempt++) {
    const res = http.post(
      `${BASE}/store/sessions/${tournamentId}/tables`,
      null,
      { headers: bearer(ownerToken), tags: { step: 'create-table' } },
    );
    if (res.status >= 200 && res.status < 300) return JSON.parse(res.body);
    if (res.status !== 409) {
      fail(`테이블 추가 실패 (${res.status}): ${String(res.body).slice(0, 200)}`);
    }
    if (onRetry) onRetry();
    // 같은 간격으로 다시 부딪히지 않게 흩는다.
    sleep((50 + Math.random() * 150) / 1000);
  }
  fail('테이블 추가가 10번 연속 409로 거절됐다');
}
```

`api.js` 상단 import에 `sleep`을 더한다.

```js
import { fail, sleep } from 'k6';
```

- [ ] **Step 2: 모니터에 연속 판정과 중단을 넣는다**

`load/lib/monitor.js` 상단 import에 두 줄을 더한다.

```js
import exec from 'k6/execution';
```

`BREACH_MS` 선언 아래에 붙인다.

```js
/**
 * 몇 번 연속으로 넘어야 멈추나. **한 번 튄 것과 무너지는 것을 가른다.**
 *
 * GC 한 번이나 램프 단계 전환의 몰림으로도 한 샘플은 튈 수 있다. 거기서
 * 멈추면 정원이 실제보다 낮게 나온다.
 */
const BREACH_STREAK = Number(__ENV.LOAD_BREACH_STREAK || 2);
let breachStreak = 0;
```

`sample()`의 `if (lag >= BREACH_MS)` 블록을 대체한다.

```js
  if (lag >= BREACH_MS) {
    lagBreaches.add(1, tags);
    breachStreak += 1;
    console.error(
      `[무너짐] ${label} · lag p95 ${lag}ms (기준 ${BREACH_MS}) · CPU ${m.cpu.percent}% · rss ${m.memoryMb.rss}MB · 연속 ${breachStreak}`,
    );
    if (breachStreak >= BREACH_STREAK) {
      // k6는 VU 간 상태를 공유하지 않으므로 각자 자기가 본 것으로 멈춘다.
      // 서버 lag은 모니터만 보고, 클라이언트 지연은 테이블 VU가 본다.
      exec.test.abort(`서버 lag p95가 ${BREACH_MS}ms를 ${breachStreak}번 연속 넘었다 (${label})`);
    }
  } else if (lag >= WARN_MS) {
    lagWarnings.add(1, tags);
    breachStreak = 0;
    console.warn(
      `[지연 감지] ${label} · lag p95 ${lag}ms (기준 ${WARN_MS}) · CPU ${m.cpu.percent}%`,
    );
  } else {
    breachStreak = 0;
  }
```

- [ ] **Step 3: 단계 라벨을 실제 VU 수에서 읽는다**

같은 파일 맨 아래에 붙인다. `watch()`는 그대로 둔다.

```js
/**
 * 지금 단계의 이름. **예정표가 아니라 실제 상태에서 읽는다.**
 *
 * `exec.instance.vusActive`가 곧 지금 도는 테이블 수다(VU 하나가 테이블
 * 하나 + 모니터 하나). 직전 호출과 값이 같으면 고원, 다르면 증설 중이다.
 *
 * 예정된 stage 시각으로 계산하지 않는 이유: 증설이 느려지면(로그인이 밀리면)
 * 예정표와 실제가 어긋나고, 그때 태그가 거짓말을 한다 — 램프가 무너지는
 * 구간이 바로 그 구간이다.
 */
let lastVus = -1;
export function stepLabel() {
  const vus = exec.instance.vusActive;
  const label = vus === lastVus ? `steady-${vus}` : `grow-${vus}`;
  lastVus = vus;
  return label;
}
```

**주의**: `stepLabel()`은 호출할 때마다 상태를 바꾼다. 테이블 VU는 액션마다 부르므로 이 모듈 상태를 공유하면 안 된다 — k6는 VU마다 모듈 인스턴스를 따로 가지므로 실제로 격리된다. 그래도 오해를 막으려고 테이블 VU 쪽은 자기 라벨 함수를 따로 둔다(다음 스텝).

- [ ] **Step 4: 램프 시나리오를 쓴다**

`load/scenarios/ramp.js`를 새로 만든다.

```js
import { sleep } from 'k6';
import exec from 'k6/execution';
import { Counter, Trend } from 'k6/metrics';
import { createTableWithRetry, login, startTournament } from '../lib/api.js';
import { sample, stepLabel as monitorLabel } from '../lib/monitor.js';
import { buildSummary } from '../lib/summary.js';
import { runHands, seatPlayers } from '../lib/table.js';

/**
 * 성장 램프 — 붙은 테이블은 내려가지 않는다.
 *
 * 답하려는 것이 둘이다. **동시접속을 어디까지 받나**(고원 구간)와 **몰림을
 * 어디까지 견디나**(증설 구간). 둘은 같은 실행의 다른 구간이라 태그로 가른다.
 *
 * 계단 램프(단계마다 세우고 재고 내린다)를 버린 이유: 재는 것이 "N테이블의
 * 부하"가 아니라 "N테이블을 세우는 비용"이 된다.
 *
 *   LOAD_TABLES_PER_STORE 미설정 → 램프 B. 대회 하나 안에서 테이블을 늘린다
 *                                  (`SEAT_LIST_UPDATED`의 O(T²) 곡선)
 *   LOAD_TABLES_PER_STORE=6      → 램프 A. 6테이블마다 다음 대회로 넘어간다
 *                                  ("홀덤펍이 하나 더 생겼다")
 */

const manifest = JSON.parse(open('/load/.load-seed.json'));

const SEAT_COUNT = 9;
const START_TABLES = Number(__ENV.LOAD_START_TABLES || 4);
const STEP_TABLES = Number(__ENV.LOAD_STEP_TABLES || 2);
const MAX_TABLES = Number(__ENV.LOAD_MAX_TABLES || 66);
/** 한 단계를 늘리는 데 주는 시간. 증설 자체가 몰림이라 짧을수록 험한 부하다. */
const GROW_S = Number(__ENV.LOAD_GROW_S || 30);
/** 고원. 핸드 하나가 약 2분이라 그보다 짧으면 딜링 대기만 잡는 단계가 생긴다. */
const STEADY_S = Number(__ENV.LOAD_STEADY_S || 180);
/** 0이면 램프 B(전부 한 대회). 6이면 램프 A. */
const TABLES_PER_STORE = Number(__ENV.LOAD_TABLES_PER_STORE || 0);
const NAME = __ENV.LOAD_RAMP_NAME || (TABLES_PER_STORE > 0 ? 'ramp-a' : 'ramp-b');

/** 사용자가 "이거 왜 이래"라고 말하는 지점. HCI 응답 한계의 1초다. */
const MY_ACTION_ABORT_MS = Number(__ENV.LOAD_MY_ACTION_ABORT_MS || 1000);
const ABORT_STREAK = Number(__ENV.LOAD_BREACH_STREAK || 2);

/** 테이블 생성이 409로 거절된 횟수. 상점 콘솔에서는 안 보이는 값이다. */
export const tableCreateConflicts = new Counter('table_create_conflicts');
/** 한 VU가 자기 테이블을 열고 좌석 아홉을 채우는 데 걸린 시간. 증설 비용이다. */
export const tableSetupMs = new Trend('table_setup_ms', true);

function stages() {
  const out = [];
  for (let n = START_TABLES; n <= MAX_TABLES; n += STEP_TABLES) {
    out.push({ target: n, duration: `${GROW_S}s` });
    out.push({ target: n, duration: `${STEADY_S}s` });
  }
  return out;
}

const RAMP_STAGES = stages();
const TOTAL_S = RAMP_STAGES.reduce((sum, s) => sum + Number(s.duration.replace('s', '')), 0);

export const options = {
  scenarios: {
    tables: {
      executor: 'ramping-vus',
      startVUs: 0,
      stages: RAMP_STAGES,
      // **아무도 내려가지 않는다.** 누적이라야 소켓 수가 실제로 쌓인다.
      gracefulRampDown: '0s',
      exec: 'table',
    },
    monitor: {
      executor: 'per-vu-iterations',
      vus: 1,
      iterations: 1,
      maxDuration: `${TOTAL_S + 60}s`,
      exec: 'monitor',
    },
  },
  thresholds: {
    // 합격선. 실행 전체 누적이라 중단 판정에는 못 쓰지만 기록으로 남는다.
    my_action_ms: ['p(95)<200'],
    others_action_ms: ['p(95)<500'],
    socket_errors: ['count==0'],
    hands_played: ['count>0'],
  },
};

export function setup() {
  const ownerToken = login(manifest.ownerNickname, manifest.password);
  // 램프가 쓸 대회를 전부 시작시킨다. 시작해야 Redis에 블라인드 메타가 서고
  // `startPreFlop`이 통과한다(`dealer.service.ts:193`). 컨테이너의
  // MIN_PLAYERS_TO_START=2가 인원 게이트를 비켜 준다.
  manifest.tournaments.forEach((t) => startTournament(ownerToken, t.id));
  // 실행마다 다른 접두사. 닉네임이 유니크라 시드를 다시 돌리지 않고
  // 재실행하면 `POST /auth/join`이 400으로 거절한다. 네 글자로 자른다 —
  // 닉네임은 3~10자이고 뒤에 VU와 좌석이 붙는다.
  const runId = Math.random().toString(36).slice(2, 6);
  return { ownerToken, runId, endAt: Date.now() + TOTAL_S * 1000 };
}

export function table(data) {
  // **VU가 아니라 반복 순번으로 자리를 정한다.** 각 VU는 실행이 끝날 때까지
  // 반복 하나를 붙들고 있으므로 순번이 곧 테이블 번호이고, `__VU`와 달리
  // 시나리오가 둘이어도 겹치지 않는다.
  const index = exec.scenario.iterationInTest;

  // 램프 B는 전부 첫 대회다(perStore = Infinity → storeIdx = 0).
  // 램프 A는 6테이블마다 다음 대회로 넘어간다.
  const perStore = TABLES_PER_STORE > 0 ? TABLES_PER_STORE : Infinity;
  const storeIdx = Math.min(
    Math.floor(index / perStore),
    manifest.tournaments.length - 1,
  );
  const tournament = manifest.tournaments[storeIdx];
  const localIdx = index - storeIdx * (TABLES_PER_STORE > 0 ? TABLES_PER_STORE : 0);

  const setupStart = Date.now();
  // 그 대회의 첫 VU는 시드가 열어 둔 테이블을 쓰고, 나머지는 직접 연다.
  // 여는 것도 부하다 — 상점 콘솔의 버튼과 같은 경로다.
  const table =
    localIdx === 0
      ? tournament.tables[0]
      : createTableWithRetry(data.ownerToken, tournament.id, () =>
          tableCreateConflicts.add(1),
        );

  // 회원가입 → 로그인 → 결제 → OTP → 착석. 전부 제품 경로다.
  const players = seatPlayers({
    tournamentId: tournament.id,
    tableId: table.id,
    password: manifest.password,
    seatCount: SEAT_COUNT,
    // 접두사 4 + 순번(base36) + 좌석(1). 순번 66까지는 base36으로 두 글자다.
    prefix: `${data.runId}${index.toString(36)}`,
    // 테이블끼리 겹치면 `@@unique([tournamentId, userId])`가 409로 막는다.
    poolBase: index * SEAT_COUNT,
    accountPrefix: manifest.accountPrefix,
    accountPool: manifest.accountPool,
  });
  tableSetupMs.add(Date.now() - setupStart);

  // 이 VU의 롤링 창. 최근 30건의 p95가 불편선을 연속으로 넘으면 멈춘다.
  // 실행 전체 누적인 `thresholds`로는 못 하는 판정이다 — 성장 램프에서는
  // 앞 단계의 좋은 표본이 뒤 단계의 나쁜 표본을 희석한다.
  let lastVus = -1;
  function label() {
    const vus = exec.instance.vusActive;
    const l = vus === lastVus ? `steady-${vus}` : `grow-${vus}`;
    lastVus = vus;
    return l;
  }

  // 남은 시간만큼만 돈다. 늦게 붙은 VU도 같은 시각에 끝난다.
  return runHands({
    tournamentId: tournament.id,
    tableId: table.id,
    dealerOtp: manifest.dealerOtp,
    players,
    durationMs: Math.max(5000, data.endAt - Date.now()),
    bigBlind: manifest.bigBlind,
    stepLabel: label,
    onMyAction: makeAbortWatcher(),
  });
}

/**
 * 내 액션 지연의 롤링 창. 최근 30건의 p95가 불편선을 `ABORT_STREAK`번
 * 연속으로 넘으면 실행을 멈춘다.
 */
function makeAbortWatcher() {
  const window = [];
  let streak = 0;
  return (elapsed) => {
    window.push(elapsed);
    if (window.length > 30) window.shift();
    if (window.length < 30) return;
    const sorted = [...window].sort((a, b) => a - b);
    const p95 = sorted[Math.floor(sorted.length * 0.95)];
    if (p95 > MY_ACTION_ABORT_MS) {
      streak += 1;
      if (streak >= ABORT_STREAK) {
        exec.test.abort(
          `내 액션 p95가 ${MY_ACTION_ABORT_MS}ms를 ${streak}번 연속 넘었다 ` +
            `(테이블 ${exec.instance.vusActive}개, 최근 30건 p95 ${p95}ms)`,
        );
      }
    } else {
      streak = 0;
    }
  };
}

export function monitor() {
  // 첫 샘플은 `watch`가 버린다 — 직전 창이 부팅부터 지금까지라 램프와
  // 무관한 구간이다.
  watchRamp();
}

function watchRamp() {
  const every = Number(__ENV.LOAD_SAMPLE_MS || 5000);
  const until = Date.now() + TOTAL_S * 1000;
  sample('warmup');
  while (Date.now() < until) {
    sleepMs(every);
    sample(monitorLabel());
  }
}

// k6의 `sleep`은 초 단위다. 밀리초로 다루는 다른 값들과 섞이지 않게 감싼다.
import { sleep } from 'k6';
function sleepMs(ms) {
  sleep(ms / 1000);
}

export function handleSummary(data) {
  return buildSummary(data, NAME);
}
```

- [ ] **Step 5: `runHands`가 `onMyAction`을 부르게 한다**

`load/lib/table.js`의 시그니처에 `onMyAction`을 더하고, 지연 기록에서 내 액션일 때 부른다.

```js
export function runHands({
  tournamentId,
  tableId,
  dealerOtp,
  players,
  durationMs,
  bigBlind,
  stepLabel,
  onMyAction,
}) {
```

```js
        if (idx === win.actorSocketIdx) {
          myActionMs.add(elapsed, tags);
          // 중단 판정은 호출자가 한다. 이 모듈은 창을 모른다.
          if (onMyAction) onMyAction(elapsed);
        } else othersActionMs.add(elapsed, tags);
```

- [ ] **Step 6: 요약에 램프 값을 더한다**

`load/lib/summary.js`의 `buildSummary`에서 `summary` 객체에 네 줄을 더한다.

```js
    raises: counter(data, 'raises'),
    folds: counter(data, 'folds'),
    rebuysAccepted: counter(data, 'rebuys_accepted'),
    tableCreateConflicts: counter(data, 'table_create_conflicts'),
    tableSetupMs: trend(data, 'table_setup_ms'),
```

`oneLine`의 배열에 두 줄을 더한다(`자리비움` 뒤).

```js
    `레이즈 ${summary.raises}/폴드 ${summary.folds}/리바인 ${summary.rebuysAccepted}`,
    `테이블409 ${summary.tableCreateConflicts}`,
```

- [ ] **Step 7: npm 스크립트를 더한다**

`package.json`의 `scripts`에 두 줄. `load:smoke` 아래에 둔다.

```json
    "load:ramp-b": "docker compose -f backend/docker-compose.test.yml --profile load --profile k6 run --rm k6 run --out json=/load/results/ramp-b-raw.json /load/scenarios/ramp.js",
    "load:ramp-a": "docker compose -f backend/docker-compose.test.yml --profile load --profile k6 run --rm k6 run --out json=/load/results/ramp-a-raw.json -e LOAD_TABLES_PER_STORE=6 /load/scenarios/ramp.js",
```

- [ ] **Step 8: 짧은 램프로 배선을 확인한다**

**측정이 아니라 배선 확인이다.** 4→6테이블 두 단계만, 고원 40초로 돌린다.

```bash
npm run seed:load
docker compose -f backend/docker-compose.test.yml --profile load --profile k6 run --rm k6 \
  run --out json=/load/results/ramp-b-raw.json \
  -e LOAD_MAX_TABLES=6 -e LOAD_GROW_S=20 -e LOAD_STEADY_S=40 \
  /load/scenarios/ramp.js
```

Expected:
- 요약 한 줄에 `핸드 > 0`, `소켓오류 0`, `리바인 > 0`
- `load/results/ramp-b-raw.json`에 `grow-4` · `steady-4` · `grow-6` · `steady-6` 태그가 전부 있다

```bash
node -e "const fs=require('fs');const seen=new Set();fs.readFileSync('load/results/ramp-b-raw.json','utf8').split('\n').forEach(l=>{if(!l)return;try{const o=JSON.parse(l);if(o.data&&o.data.tags&&o.data.tags.step)seen.add(o.data.tags.step)}catch(e){}});console.log([...seen].sort().join(' '))"
```

Expected: `grow-4 grow-5 grow-6 login … steady-4 steady-6 …` — `steady-N`이 하나도 없으면 단계 태그가 안 붙은 것이므로 **멈추고 고친다.**

- [ ] **Step 9: 램프 A 배선도 확인한다**

```bash
npm run seed:load
docker compose -f backend/docker-compose.test.yml --profile load --profile k6 run --rm k6 \
  run -e LOAD_TABLES_PER_STORE=6 -e LOAD_MAX_TABLES=8 -e LOAD_GROW_S=20 -e LOAD_STEADY_S=40 \
  /load/scenarios/ramp.js
```

Expected: 소켓오류 0, 핸드 > 0. 그리고 대회 둘에 걸쳐 앉았는지 확인한다 — 7·8번 테이블은 두 번째 대회다.

```bash
docker compose -f backend/docker-compose.test.yml exec -T db-test \
  psql -U test -d playsync_test -c \
  "SELECT t.name, count(distinct tb.id) AS 테이블, count(p.id) AS 참가 \
   FROM \"Tournament\" t LEFT JOIN \"Table\" tb ON tb.\"tournamentId\"=t.id \
   LEFT JOIN \"TournamentParticipation\" p ON p.\"tournamentId\"=t.id \
   GROUP BY t.name ORDER BY t.name;"
```

Expected: 대회 둘에 참가자가 나뉘어 있다.

- [ ] **Step 10: 커밋**

```bash
git add load/scenarios/ramp.js load/lib/api.js load/lib/monitor.js load/lib/summary.js load/lib/table.js package.json
git commit -m "feat: 성장 램프 — 붙은 테이블은 내려가지 않는다"
```

---

### Task 5: 재접속 폭발

T31이 만든 복구 경로의 뒷면이다 — 서버가 살아 돌아오거나 행사장 AP가 한 번 끊겼다 붙으면 정확히 이 모양이 된다. `SeatGameClient.tsx:118`에 자동 재접속이 없어 실제 모양은 "사람들이 배너를 보고 동시에 새로고침"이고, 여기엔 지터가 걸려 있지 않다.

별도 실행으로 두지 않는다 — 규모 축이 하나 더 생기고 실행 시간만 배가 된다. 고원 중에 한 번 일어나는 사건으로 넣는다.

**Files:**
- Modify: `load/lib/table.js` (`runHands`에 재접속 인자)
- Modify: `load/scenarios/ramp.js` (언제 터뜨릴지)

**Interfaces:**
- Consumes: 태스크 4의 `runHands({..., onMyAction, stepLabel})`
- Produces: `reconnects` Counter, `reconnect_ms` Trend

- [ ] **Step 1: 지표를 더한다**

`load/lib/table.js`의 카운터 묶음에 붙인다.

```js
/** 소켓을 통째로 끊었다 다시 붙인 횟수. 재접속 폭발이 실제로 일어났다는 증거다. */
export const reconnects = new Counter('reconnects');
/** 전원이 다시 붙어 첫 renderGame을 받기까지. 티켓 발급 왕복이 포함된다. */
export const reconnectMs = new Trend('reconnect_ms', true);
```

- [ ] **Step 2: 재접속을 `runHands` 안에 넣는다**

`runHands`의 인자에 `reconnectAtMs`를 더하고(밀리초, 시작 기준 상대 시각. `null`이면 안 한다), `kick` 타이머 선언 아래에 붙인다.

```js
  // 재접속 폭발. **티켓은 1회용이라 소켓마다 새로 받아야 한다**(T24 —
  // Redis GETDEL로 소비된다). 그래서 이 사건은 WS만이 아니라 REST
  // (`POST /ws/ticket`)를 전원이 동시에 치는 부하이기도 하다.
  //
  // 지터를 걸지 않는다. 실제 모양이 "배너를 본 사람들이 동시에
  // 새로고침"이고, `SeatGameClient.tsx:118`에 자동 재접속이 없어
  // 백오프가 낄 자리 자체가 없다.
  const burst =
    reconnectAtMs === null || reconnectAtMs === undefined
      ? null
      : setTimeout(() => {
          if (closing) return;
          const at = Date.now();
          const old = sockets.splice(0, sockets.length);
          old.forEach((s) => {
            try {
              s.ws.close();
            } catch (e) {
              // 이미 닫힌 소켓. 다시 붙는 중이라 무시한다.
            }
          });
          // 창을 비운다 — 끊긴 소켓이 못 받은 창은 영영 안 채워진다.
          windows.length = 0;
          reconnects.add(1);
          seats.forEach((s) => open(wsTicket(s.seatToken), 'seat', s.seat));
          open(wsTicket(dealerToken), 'dealer', -1);
          reconnectMs.add(Date.now() - at);
        }, reconnectAtMs);
```

정리 타이머에서 `clearTimeout(burst)`를 부른다.

```js
      closing = true;
      clearTimeout(kick);
      if (burst) clearTimeout(burst);
```

- [ ] **Step 3: 램프가 언제 터뜨릴지 정한다**

`load/scenarios/ramp.js`에 상수를 더한다.

```js
/**
 * 재접속 폭발을 일으킬 테이블 수. 그 단계의 고원 중간에 전원이 끊었다 붙는다.
 * 0이면 하지 않는다.
 */
const RECONNECT_AT_TABLES = Number(__ENV.LOAD_RECONNECT_AT_TABLES || 0);
```

`table()`의 `runHands` 호출에 인자를 더한다. 그 단계의 고원 중간 시각을 실행 시작 기준으로 계산한다 — 모든 VU가 같은 절대 시각을 보므로 **동시에** 터진다.

```js
  // 그 단계의 고원 중간. `setup`이 잡은 시작 시각 기준이라 전 VU가 같은
  // 순간에 끊는다 — 그게 "배너를 본 사람들이 동시에 새로고침"의 모양이다.
  let reconnectAtMs = null;
  if (RECONNECT_AT_TABLES > 0) {
    const stepsBefore = Math.max(
      0,
      Math.ceil((RECONNECT_AT_TABLES - START_TABLES) / STEP_TABLES),
    );
    const absolute =
      data.startedAt + (stepsBefore + 1) * (GROW_S + STEADY_S) * 1000 - (STEADY_S / 2) * 1000;
    const delta = absolute - Date.now();
    // 이미 지난 시각이면 이 VU는 그 단계 뒤에 붙은 것이다 — 하지 않는다.
    if (delta > 0) reconnectAtMs = delta;
  }
```

`setup()`의 반환에 `startedAt`을 더한다.

```js
  return { ownerToken, runId, startedAt: Date.now(), endAt: Date.now() + TOTAL_S * 1000 };
```

`runHands` 호출에:

```js
    reconnectAtMs,
```

- [ ] **Step 4: 짧은 램프로 확인한다**

```bash
npm run seed:load
docker compose -f backend/docker-compose.test.yml --profile load --profile k6 run --rm k6 \
  run -e LOAD_MAX_TABLES=6 -e LOAD_GROW_S=20 -e LOAD_STEADY_S=60 \
  -e LOAD_RECONNECT_AT_TABLES=6 /load/scenarios/ramp.js
```

Expected: `reconnects` == 붙어 있던 VU 수(6), `reconnect_ms`에 값이 있고, **`socket_errors` == 0**.

소켓 오류가 나면 우리가 닫은 것을 `onclose`가 세고 있는 것이다 — 재접속 시에는 `closing` 플래그가 꺼져 있으므로 코드 1000이 아닌 종료가 잡힌다. 그때는 재접속 직전에 잠깐 세우지 않도록 플래그를 하나 더 둔다.

- [ ] **Step 5: 커밋**

```bash
git add load/lib/table.js load/scenarios/ramp.js
git commit -m "feat: 재접속 폭발을 고원 한가운데에 넣는다"
```

---

### Task 6: 실행과 결과

여기부터는 코드가 아니라 **측정**이다. 실행이 길다(램프 하나가 붕괴까지 최대 두 시간). 백그라운드로 돌리고 결과 파일을 읽는다.

**Files:**
- Create: `docs/results/2026-08-15-load-ramp.md`
- Modify: `docs/tickets-next.md` (T41 절 추가)
- Modify: `docs/backlog.md` (B11 상태 갱신)
- Modify: `load/README.md` (램프 절 추가)
- Modify: `CLAUDE.md` (명령어 표에 `load:ramp-a` · `load:ramp-b`)
- Delete: `docs/handoff.md` (T41이 그것을 소진했다)

**Interfaces:**
- Consumes: 태스크 4·5가 만든 시나리오와 스크립트
- Produces: 결과 문서. B9(WS 수평 확장)의 판단 근거가 된다.

- [ ] **Step 1: 램프 B를 돌린다**

```bash
npm run load:down && npm run load:up && npm run seed:load
npm run load:ramp-b
```

붕괴선이나 불편선에 닿으면 k6가 스스로 멈추고 중단 사유를 로그에 남긴다. 중단 사유 문구를 그대로 받아 적는다 — 어느 선이 먼저 왔는지가 결과의 핵심이다.

- [ ] **Step 2: 램프 B의 단계별 수치를 뽑는다**

원시 시계열에서 단계별로 갈라 읽는다.

```bash
node scripts/load-report.mjs load/results/ramp-b-raw.json
```

이 스크립트가 없으므로 같이 만든다. `scripts/load-report.mjs`:

```js
// 원시 시계열(k6 --out json)을 단계 태그로 갈라 한 줄씩 낸다.
//
// 요약 하나로는 "터졌다"까지만 알 수 있다. 필요한 것은 언제부터 지연이
// 보이기 시작했고 언제 급해졌는가이고, 그건 단계별로 놓아야 보인다.
import { readFileSync } from 'fs';

const path = process.argv[2];
if (!path) {
  console.error('사용법: node scripts/load-report.mjs <raw.json>');
  process.exit(1);
}

/** 지표별 · 단계별 표본 모음 */
const buckets = new Map();
for (const line of readFileSync(path, 'utf8').split('\n')) {
  if (!line) continue;
  let row;
  try {
    row = JSON.parse(line);
  } catch {
    continue;
  }
  if (row.type !== 'Point' || !row.data || !row.data.tags) continue;
  const step = row.data.tags.step;
  if (!step || !/^(grow|steady)-\d+$/.test(step)) continue;
  const key = `${step} ${row.metric}`;
  if (!buckets.has(key)) buckets.set(key, []);
  buckets.get(key).push(row.data.value);
}

function p95(values) {
  if (!values || values.length === 0) return null;
  const s = [...values].sort((a, b) => a - b);
  return Math.round(s[Math.floor(s.length * 0.95)] * 100) / 100;
}

const steps = [...new Set([...buckets.keys()].map((k) => k.split(' ')[0]))].sort(
  (a, b) => {
    const [ka, na] = a.split('-');
    const [kb, nb] = b.split('-');
    return Number(na) - Number(nb) || ka.localeCompare(kb);
  },
);

console.log('단계 | 테이블 | 내액션p95 | 남의액션p95 | 서버lag p95 | 표본');
for (const step of steps) {
  const tables = step.split('-')[1];
  const my = p95(buckets.get(`${step} my_action_ms`));
  const others = p95(buckets.get(`${step} others_action_ms`));
  const lag = p95(buckets.get(`${step} server_lag_p95_ms`));
  const n = (buckets.get(`${step} my_action_ms`) || []).length;
  console.log(`${step} | ${tables} | ${my ?? '-'} | ${others ?? '-'} | ${lag ?? '-'} | ${n}`);
}
```

- [ ] **Step 3: 램프 A를 돌리고 같은 표를 뽑는다**

```bash
npm run load:down && npm run load:up && npm run seed:load
npm run load:ramp-a
node scripts/load-report.mjs load/results/ramp-a-raw.json
```

- [ ] **Step 4: 계정 풀이 모자라지 않았는지 확인한다**

```bash
node -e "const a=require('./load/results/ramp-a-latest.json'),b=require('./load/results/ramp-b-latest.json'); \
  console.log('A 가입/로그인', a.signups, a.logins, '| B', b.signups, b.logins)"
```

가입 비율이 `LOAD_NEW_USER_RATIO`(0.1)에서 크게 벗어나 있으면 풀이 모자란 것이고, **그 실행의 정원은 낮게 나온 값이다**(bcrypt가 사람당 두 번 돌았다). 그때는 `LOAD_ACCOUNT_POOL`을 늘려 다시 돌린다.

- [ ] **Step 5: 결과 문서를 쓴다**

`docs/results/2026-08-15-load-ramp.md`. 담을 것:

| 절 | 무엇 |
|---|---|
| 측정 환경 | 백엔드 1코어 512MB, DB·Redis 무제한, k6 무제한. **`LOAD_TABLES_PER_STORE` 값을 반드시 적는다** — 이 값이 "몇 상점"의 전제라 안 적으면 숫자가 혼자 돌아다닌다 |
| 램프 B 표 | 단계별 테이블 수 · 내 액션 p95 · 남의 액션 p95 · 서버 lag p95 · 표본 수 |
| 램프 A 표 | 같은 모양, x축이 상점 |
| 어느 선이 먼저 왔나 | 불편선(내 액션 1초)과 붕괴선(lag 100ms) 중 무엇이 먼저인가. 중단 사유 문구 그대로 |
| O(T²)의 실측 | 램프 B의 곡선이 실제로 꺾이는가. 설계 문서의 계산(테이블 6 = 40KB / 40 = 1.8MB)과 대조 |
| 확장 순서의 현재 위치 | 설계 문서 1~4 중 어디까지 왔는지 |
| 램프가 드러낸 것 | 테이블 생성 409 횟수, 재접속 폭발의 복구 시간, 그 밖에 터진 것 |

**숫자를 지어내지 않는다.** 실제 실행이 만든 파일에서만 옮긴다.

- [ ] **Step 6: 기록을 갱신한다**

- `docs/tickets-next.md`에 T41 절. 담을 것: 계단→성장으로 바꾼 근거, 무한 리바인이 없으면 리바인 경로가 한 번도 안 돈다는 발견, `createSession`의 스냅샷 누락, 램프 중에 실제로 터진 것과 그 처리(고쳤나 티켓으로 뺐나), 남긴 것.
- `docs/backlog.md`의 B11 절: T41 완료로 갱신하고 결과 문서를 가리킨다. B9(WS 수평 확장)의 "하지 않는다" 판단에 실측 근거가 붙었음을 적는다.
- `load/README.md`에 램프 절: 실행 방법, 환경변수 표, 태그 읽는 법.
- `CLAUDE.md`의 명령어 표에 `npm run load:ramp-a` · `npm run load:ramp-b`. 베이스라인 숫자도 갱신한다(태스크 1이 통합 테스트 하나를 더했다).
- `docs/handoff.md` 삭제. T41이 그 문서의 내용을 소진했다.

- [ ] **Step 7: 베이스라인을 실제로 확인한다**

```bash
npm run load:down
npm run typecheck
npm run test
npm run test:int
```

Expected: 타입 에러 0, 단위·통합 전부 통과. 통합은 343(342 + 태스크 1의 한 건)이어야 한다. **실제 출력 숫자를 CLAUDE.md에 적는다.**

- [ ] **Step 8: 커밋**

```bash
git add docs/results/2026-08-15-load-ramp.md docs/tickets-next.md docs/backlog.md load/README.md CLAUDE.md scripts/load-report.mjs
git rm docs/handoff.md
git commit -m "docs: 램프가 잰 것을 남긴다"
```

---

## 실행 중에 터진 것을 어떻게 다루나

램프는 제품을 처음으로 진짜 동시성 아래 두는 자리다. 갈래를 둘로 나눈다.

| | 어떻게 |
|---|---|
| 장부 · 락 · 트랜잭션 경계 | **우선 고치고 진행한다.** 하네스를 완주하는 것보다 장부가 먼저다 |
| 나머지 | 티켓으로 적고 램프를 계속 돌린다 |

이미 알고 있는 후보 둘.

- **T42(레이스 컨디션 전수조사).** 스냅샷을 고치는 열한 경로가 "락 안에서 다시 읽는다"를 각자 손으로 지킨다. 램프가 그 창을 실제로 열면 T42를 앞으로 당긴다.
- **등록 마감 권위 이원화**(T31 이월). 자동 마감이 Redis에만 반영되고 결제 게이트는 DB 컬럼을 본다. 램프가 `rebuyUntil`을 넘길 만큼 길면 드러난다 — 지금 시드는 600분이라 안 넘는다.

## 세션이 모자라면 어디서 자르나

순서대로 자른다. 앞의 것이 없으면 뒤가 의미 없다.

1. **태스크 5(재접속 폭발)** — 성장 램프의 본선과 독립이다. T43으로 뺀다.
2. **램프 A** — 램프 B(O(T²) 곡선)가 설계 문서가 말한 "부하테스트의 첫 산출물"이다. 램프 A만 T43으로 뺀다.
3. 태스크 1~4와 램프 B 실행은 자르지 않는다. 여기까지가 T41이 "끝났다"고 말할 수 있는 최소다.
