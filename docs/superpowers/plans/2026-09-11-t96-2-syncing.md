# T96 PR 2 — SYNCING Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 부팅이 진행 중 대회를 `SYNCING`으로 두고 블라인드를 멈추며, 그 대회의 딜러 태블릿이 전부 돌아오면(n/n) 자동으로 `ONGOING`으로 되돌리고 멈춘 시간만큼 한 번 민다. REST 화면(참가자 폰 · 전광판 · 콘솔)과 딜러 화면이 그 상태를 보여준다.

**Architecture:** `Tournament.status = SYNCING`과 `Tournament.pausedAt`이 한 쌍이다(같은 update로만 쓴다). 레벨 계산은 `pausedAt`이 있으면 그 시각에서 멈춘다. 켜는 자리는 `RecoveryService.recoverAll`, 끄는 자리는 `RecoveryService.completeSync` 하나다. 게이트웨이가 딜러 소켓을 세어 n/n을 판정하고(순수 함수 `syncProgress`), `SYNCING` 동안 딜러 명령을 거절한다.

**Tech Stack:** NestJS · Prisma(PostgreSQL) · ioredis · `ws` · Next.js · zod contract · jest · vitest

**Spec:** `docs/superpowers/specs/2026-09-11-t96-syncing-design.md` 2부

**전제:** PR 1(`2026-09-11-t96-1-zombie-socket.md`)이 먼저 머지되어 있다. 이 브랜치는 그 위에서 딴다.

## Global Constraints

- `status === SYNCING` ⇔ `pausedAt !== null`. 두 필드를 쓰는 자리는 `recoverTournament`(켬)와 `completeSync`(끔) 둘뿐이고 둘 다 **한 문장**으로 둘을 함께 쓴다.
- 보정은 `completeSync` 한 곳에서만 한다. 부팅은 **밀지 않는다.**
- Redis 블라인드 기준점은 부팅마다 **DB에서 대입**한다: `startedAt + pausedMs`, `pausedAt`도 DB 값 그대로.
- `ONGOING`과 직접 비교하지 않는다. 살아 있는 대회는 `LIVE_TOURNAMENT_STATUSES`(`ONGOING` · `SYNCING`) / `isLiveTournament`.
- 딜러 명령 거절 문구: `딜러가 모두 돌아올 때까지 기다려 주세요.`
- 화면 라벨: `SYNCING` → `복구 중`.
- 문서(`docs/`, `CLAUDE.md`)는 하위 에이전트가 건드리지 않는다. 주석은 코드라 함께 간다. 좌표는 이름으로.
- 새 테스트는 실패를 먼저 본다. 사후 검사는 제품 코드를 되돌려 빨간불을 확인한다.

---

### Task 1: 상태 축 — `SYNCING` · `pausedAt` · 살아 있는 상태 목록

**Files:**
- Create: `backend/prisma/migrations/20260911120000_tournament_syncing/migration.sql`
- Modify: `backend/prisma/schema.prisma` (`TournamentStatus`, `Tournament.pausedAt`)
- Create: `packages/contract/src/tournament-status.ts` · Modify: `packages/contract/src/index.ts`
- Create: `packages/contract/src/tournament-status.spec.ts`
- Modify: `backend/src/store/session/tournament-status.ts` · `backend/src/store/session/tournament-status.spec.ts`
- Modify: `backend/src/recovery/recovery.service.ts` (조회 대상만. 나머지는 Task 3)
- Modify: `backend/src/dealer/dealer.service.ts` (딜러 로그인의 `tournament.status === 'ONGOING'`)

**Interfaces:**
- Produces: Prisma `TournamentStatus.SYNCING`, `Tournament.pausedAt: Date | null`. contract `TournamentStatusSchema`(enum 다섯), `type TournamentStatus`. 백엔드 `LIVE_TOURNAMENT_STATUSES`, `isLiveTournament(status): boolean`.

- [ ] **Step 1: 분할 검사를 먼저 쓴다**

`backend/src/store/session/tournament-status.spec.ts`에 추가:

```ts
import { TournamentStatus } from '@prisma/client';
import { TournamentStatusSchema } from '@playsync/contract';
import { CLOSED_TOURNAMENT_STATUSES, LIVE_TOURNAMENT_STATUSES } from './tournament-status';

/**
 * **모든 상태가 정확히 한 칸에 든다** — 시작 전 · 살아 있음 · 닫힘.
 *
 * `SYNCING`이 한 번 그랬다: 선언만 되고 어느 목록에도 없어서, 붙는 순간
 * 대회가 조회에서 사라지게 돼 있었다(T71). 다음 상태가 어느 칸에도 안
 * 들어가면 여기서 빨개진다.
 */
it('Prisma의 대회 상태가 시작 전 · 살아 있음 · 닫힘으로 서로소 분할된다', () => {
  const buckets = [[TournamentStatus.PENDING], [...LIVE_TOURNAMENT_STATUSES], [...CLOSED_TOURNAMENT_STATUSES]];
  const flat = buckets.flat();
  expect(new Set(flat).size).toBe(flat.length);
  expect([...flat].sort()).toEqual([...Object.values(TournamentStatus)].sort());
});

it('contract의 상태 목록이 Prisma enum과 같다', () => {
  expect([...TournamentStatusSchema.options].sort()).toEqual([...Object.values(TournamentStatus)].sort());
});
```

- [ ] **Step 2: 실패 확인**

Run: `npm test -w backend -- tournament-status.spec`
Expected: FAIL — `LIVE_TOURNAMENT_STATUSES`·`TournamentStatusSchema` 없음

- [ ] **Step 3: 스키마와 마이그레이션**

`schema.prisma`의 `enum TournamentStatus`에 `ONGOING` 다음 줄:

```prisma
  /// 서버가 멈췄다 돌아와 **그 대회의 딜러 태블릿이 다 돌아오기를 기다리는 중**(T96).
  /// 부팅이 켜고(`RecoveryService.recoverTournament`), 딜러가 n/n이 되면
  /// `RecoveryService.completeSync`가 끈다. 이 동안 블라인드 시계가 멈춘다.
  ///
  /// T71이 「아무도 대입하지 않는다」로 지웠던 값이다. 되살리면서 대입하는
  /// 자리가 둘 생겼다.
  SYNCING
```

`model Tournament`의 `pausedMs` 아래:

```prisma
  /// 정지가 시작된 시각. **정지 중이 아니면 null**(T96).
  ///
  /// `pausedMs`(누적 정지)와 스톱워치 한 쌍이다. `status === SYNCING`과
  /// 언제나 함께 서고 함께 사라진다 — 쓰는 자리 둘(`recoverTournament`·
  /// `completeSync`)이 같은 update 한 문장으로 둘을 쓴다.
  ///
  /// 값은 **마지막 하트비트**다. 복구 중 다시 죽어도 덮지 않으므로, 첫 정지 ·
  /// 복구 대기 · 두 번째 정지가 한 번의 보정에 들어간다.
  pausedAt DateTime?
```

`migration.sql` (PG 12+에서 `ADD VALUE`는 트랜잭션 안에서도 되지만, 같은 트랜잭션 안에서 그 새 값을 **쓰면** 에러다. 이 마이그레이션은 값을 쓰지 않는다):

```sql
-- T96: 서버 복구 중 상태를 되살리고, 정지 시작 시각을 둔다.
-- T71(20260821120000)이 지운 값이다. 이제 대입하는 자리가 있다.
ALTER TYPE "TournamentStatus" ADD VALUE 'SYNCING' AFTER 'ONGOING';
ALTER TABLE "Tournament" ADD COLUMN "pausedAt" TIMESTAMP(3);
```

Run: `cd backend && npx prisma generate` (마이그레이션 적용은 `npm run test:int`가 컨테이너에서 한다)

- [ ] **Step 4: contract 상태 목록**

`packages/contract/src/tournament-status.ts`:

```ts
import { z } from "zod";

/**
 * 대회 상태 전체. **프론트가 라벨을 `Record<TournamentStatus, string>`으로
 * 들게 하려고 둔다**(T96).
 *
 * 문자열로 받아 `if`로 가르면 상태가 늘 때 새 값이 폴백으로 조용히 떨어진다 —
 * `SYNCING`이 참가자 폰에 「종료」로 뜰 뻔했다. `Record`는 키가 빠지면 컴파일
 * 에러다. 값이 Prisma enum과 같은지는 백엔드 스펙이 대조한다
 * (`tournament-status.spec.ts`).
 */
export const TournamentStatusSchema = z.enum(["PENDING", "ONGOING", "SYNCING", "FINISHED", "CANCELLED"]);
export type TournamentStatus = z.infer<typeof TournamentStatusSchema>;
```

`index.ts`에 `export * from "./tournament-status";`.

`packages/contract/src/tournament-status.spec.ts`:

```ts
import { TournamentStatusSchema, ClosedTournamentStatusSchema } from "./index";

it("닫힌 상태는 전체 상태의 부분집합이다", () => {
  for (const s of ClosedTournamentStatusSchema.options) {
    expect(TournamentStatusSchema.options).toContain(s);
  }
});
```

- [ ] **Step 5: 살아 있는 상태 목록**

`backend/src/store/session/tournament-status.ts`의 `CLOSED_TOURNAMENT_STATUSES` 아래:

```ts
/**
 * **시작했고 아직 닫히지 않은 대회**(T96).
 *
 * `status === ONGOING`으로 직접 비교하던 자리들이 `SYNCING`을 조용히 놓친다 —
 * 복구 중에 다시 죽은 대회가 복구 대상에서 빠지는 것이 그 하나였다. 판정을
 * 여기 한 곳에 둔다(닫힘을 `isClosedTournament` 하나로 모은 것과 같은 이유).
 */
export const LIVE_TOURNAMENT_STATUSES = [
  TournamentStatus.ONGOING,
  TournamentStatus.SYNCING,
] as const;

export function isLiveTournament(status: TournamentStatus): boolean {
  return (LIVE_TOURNAMENT_STATUSES as readonly TournamentStatus[]).includes(status);
}
```

- [ ] **Step 6: 새던 백엔드 자리 둘**

`RecoveryService.recoverAll`의 대회 조회:

```ts
      // `SYNCING`도 대상이다(T96). 복구를 기다리는 중에 다시 죽은 대회가
      // 여기서 빠지면 그 대회는 영영 복구되지 않는다.
      const tournaments = await this.prisma.tournament.findMany({
        where: { status: { in: [...LIVE_TOURNAMENT_STATUSES] } },
        select: { id: true },
      });
```

`DealerService`의 딜러 로그인에서 `if (tournament.status === 'ONGOING') {` → `if (isLiveTournament(tournament.status)) {` (import 추가).

- [ ] **Step 7: 통과 확인**

Run: `npm run typecheck && npm test -w backend -- tournament-status && npm test -w @playsync/contract`
Expected: PASS. 타입 체크가 프론트의 `Record<ClosedTournamentStatus, …>` 등에서 새 에러를 내지 않아야 한다(아직 프론트 라벨은 문자열이다).

- [ ] **Step 8: 커밋**

```bash
git add backend/prisma packages/contract/src/tournament-status.ts packages/contract/src/tournament-status.spec.ts packages/contract/src/index.ts backend/src/store/session/tournament-status.ts backend/src/store/session/tournament-status.spec.ts backend/src/recovery/recovery.service.ts backend/src/dealer/dealer.service.ts
git commit -m "feat(T96): SYNCING과 pausedAt을 되살리고 살아 있는 상태를 한 곳에 둔다"
```

---

### Task 2: 블라인드 시계를 `pausedAt`에서 멈춘다

**Files:**
- Modify: `backend/shared/util/util.ts` (`getCurrentBlindLevel`)
- Modify: `backend/shared/types/tournamentMeta.ts` (`BlindField.pausedAt?`)
- Modify: `packages/contract/src/dashboard.ts` (`BlindFieldSchema.pausedAt`)
- Modify: `backend/src/redis/redis.service.ts` (`checkAndSyncBlindLevel`)
- Modify: `backend/src/store/session/tournament-meta.ts` (`buildTournamentMeta`)
- Modify: `backend/src/store/session/registration.ts` (`isRegistrationOpenNow`, `RegistrationSource`)
- Test: `backend/shared/util/util.spec.ts`(없으면 만든다), `backend/src/store/session/registration.spec.ts`, `backend/src/redis/redis.service.int-spec.ts`(없으면 `backend/src/recovery/recovery.service.int-spec.ts`에 둔다)

**Interfaces:**
- Consumes: Task 1의 `Tournament.pausedAt`.
- Produces: `getCurrentBlindLevel(structure, startedAt, now = Date.now())`. `BlindField.pausedAt?: number`(epoch ms). `buildTournamentMeta(game, blindBaseAt, pausedAt?: number | null)`. `RegistrationSource.pausedAt: Date | null`.

- [ ] **Step 1: 실패하는 테스트**

`getCurrentBlindLevel`(순수):

```ts
it('now를 받으면 그 시각의 레벨을 준다 — 벽시계와 무관하다', () => {
  const structure = [
    { lv: 1, sb: 100, ante: false, duration: 1 },
    { lv: 2, sb: 200, ante: false, duration: 1 },
  ];
  const startedAt = Date.now() - 90_000; // 벽시계로는 레벨 2
  expect(getCurrentBlindLevel(structure, startedAt, startedAt + 30_000).currentIndex).toBe(0);
});
```

`isRegistrationOpenNow`(`registration.spec.ts`, 기존 픽스처 모양을 따른다):

```ts
/**
 * **정지 중에는 레벨이 안 오른다.** 마감이 단조라 정지 구간에서 한 번 닫히면
 * 되돌아오지 않는다 — 보정만으로는 부족하고 계산이 멈춰야 한다(T96).
 */
it('pausedAt이 있으면 그 시각의 레벨로 판정한다', () => {
  const startedAt = new Date(Date.now() - 90_000); // 벽시계로는 마감 레벨(2)을 지났다
  const t = source({ startedAt, pausedMs: 0, rebuyUntil: 2, pausedAt: new Date(startedAt.getTime() + 30_000) });
  expect(isRegistrationOpenNow(t)).toBe(true);
});
```

`checkAndSyncBlindLevel`(Redis 통합): `blindField`를 `{ startedAt: now-90_000, pausedAt: now-60_000, currentBlindLv: 0, nextLevelAt: now-30_000, … }`로 세우고(구조는 1분 레벨 둘, `rebuyUntil`: 2), `checkAndSyncBlindLevel(id)` 뒤 `currentBlindLv === 0`이고 info 해시의 `isRegistrationOpen`이 `'1'`인지 본다.

- [ ] **Step 2: 실패 확인**

Run: `npm test -w backend -- util.spec registration.spec` · `npm run test:int -w backend -- <Redis 검사를 둔 파일>`
Expected: FAIL (레벨 1/2가 나오고 마감이 닫힌다)

- [ ] **Step 3: 구현**

`getCurrentBlindLevel`: 세 번째 인자 `now: number = Date.now()`를 받고 `const now = Date.now();` 줄을 지운다.

`BlindField`(백엔드 타입)에:

```ts
  /**
   * 정지가 시작된 시각(epoch ms). **있으면 시계가 멈춰 있다**(T96).
   * DB `Tournament.pausedAt`의 사본이다 — 부팅이 대입하고 `completeSync`가 지운다.
   */
  pausedAt?: number,
```

contract `BlindFieldSchema`에 `pausedAt: z.int().optional(),` 와 같은 뜻의 주석(전광판이 멈춘 시계와 「서버 복구 중」 화면을 그리는 근거).

`checkAndSyncBlindLevel`: `const now = Date.now();`를 다음으로:

```ts
    // 정지 중이면 시계가 그 시각에 멈춰 있다(T96). 레벨이 오르면 등록 마감이
    // 닫히고 마감은 단조라 되돌아오지 않는다 — 보정만으로는 부족하다.
    // `serverTime`은 실제 시각 그대로 둔다. 단말이 자기 시계와의 오프셋을 재는 값이다.
    const wall = Date.now();
    const now = blind.pausedAt ?? wall;
```

빠른 경로의 `serverTime: now` → `serverTime: wall`, `getCurrentBlindLevel(blind.blindStructure, blind.startedAt)` → `(…, blind.startedAt, now)`, `updatedBlind`의 `serverTime: now` → `serverTime: wall`.

`buildTournamentMeta(game, blindBaseAt, pausedAt?: number | null)`: `getCurrentBlindLevel(blindStructure, blindBaseAt, pausedAt ?? Date.now())`로 레벨을 재고, 반환하는 `blindField`에 `...(pausedAt != null ? { pausedAt } : {})`.

`isRegistrationOpenNow`: `RegistrationSource`에 `pausedAt: Date | null`을 더하고 `getCurrentBlindLevel(structure, blindBaseAt, t.pausedAt?.getTime() ?? Date.now())`. 이 타입을 쓰는 픽스처(`registration.spec.ts`의 `source`, `payment.service.int-spec.ts` 등)가 타입 에러를 내면 `pausedAt: null`을 기본값으로 더한다.

- [ ] **Step 4: 통과 확인**

Run: `npm run typecheck && npm test -w backend && npm run test:int -w backend -- <Redis 검사를 둔 파일>`
Expected: PASS

- [ ] **Step 5: 되돌려 확인**

`checkAndSyncBlindLevel`의 `const now = blind.pausedAt ?? wall;`을 `const now = wall;`로 바꿔 Redis 검사가 빨갛게 되는지 → 복원.

- [ ] **Step 6: 커밋**

```bash
git commit -am "feat(T96): 정지 중에는 블라인드 레벨을 멈춘 시각으로 잰다"
```

---

### Task 3: 복구의 수명 — 켜기 · 끄기 (동시성: 태스크 리뷰 받음)

**Files:**
- Modify: `backend/src/recovery/recovery.service.ts`
- Modify: `backend/src/recovery/recovery.module.ts` (`exports: [RecoveryService]`)
- Modify: `backend/src/recovery/recovery.service.int-spec.ts` (보정 시점이 옮겨간 검사들을 새 뜻으로)
- Modify: `backend/src/scenario/server-recovery.int-spec.ts` · `backend/src/scenario/pause-resume.int-spec.ts`
- Create: `backend/src/scenario/syncing.int-spec.ts`

**Interfaces:**
- Consumes: Task 1(`SYNCING`, `pausedAt`, `LIVE_TOURNAMENT_STATUSES`), Task 2(`buildTournamentMeta(…, pausedAt)`, `BlindField.pausedAt`).
- Produces: `RecoveryService.completeSync(tournamentId: string): Promise<boolean>` — 이 호출이 `SYNCING`을 끝냈으면 `true`. Task 4가 부른다.

- [ ] **Step 1: 시나리오를 먼저 쓴다**

`backend/src/scenario/syncing.int-spec.ts` — `pause-resume.int-spec.ts`와 같은 하네스(`setupTournament`, `checkInvariants`). 블라인드 구조는 레벨 1분짜리로, `rebuyUntil`은 2로 세운다(하네스 옵션이 없으면 `h.prisma.tournament.update` + `h.redisService.setTournamentBlind`로 맞춘다). 단계:

```ts
describe('시나리오 — SYNCING', () => {
  // 0. 판이 돈다 (startPreFlop)
  // 1. 하트비트를 3분 전으로 → recoverAll
  //    → status SYNCING, pausedAt ≈ 하트비트 시각(±1초), blindField.pausedAt 같은 값
  //    → pausedMs는 그대로(부팅은 밀지 않는다)
  // 2. SYNCING 동안 레벨이 안 오르고 등록이 안 닫힌다
  //    (하트비트를 마감 레벨 경계 **직전**으로 두는 입력 — 벽시계로는 지났다)
  //    → h.redisService.checkAndSyncBlindLevel 뒤 currentBlindLv 그대로, isRegistrationOpen '1'
  // 3. 복구 중 재시작: 하트비트를 다시 과거로 찍고 recoverAll
  //    → pausedAt이 1단계 값 그대로, 대회가 여전히 SYNCING(대상에서 안 빠졌다)
  // 4. completeSync → true. status ONGOING, pausedAt null,
  //    pausedMs 증가량 == Redis blindField.startedAt 증가량 == Δ(±1초), blindField.pausedAt 없음
  // 5. 같은 대회에 completeSync 다시 → false, pausedMs 그대로
  // 6. resumeTable → 액션이 다시 들어간다
  // 매 단계 checkInvariants(h, '<단계 이름>', chips)
});
```

반대 입력 두 개를 같은 파일에 `it`으로:
- **동시 n/n**: `Promise.all([completeSync(id), completeSync(id)])` → 결과가 `[true,false]` 순서 무관, `pausedMs` 증가가 Δ 한 번.
- **앉은 테이블이 없는 대회**: 좌석을 전부 비운(`releaseSeats` 또는 비트맵·스냅샷을 빈 상태로) 대회를 `recoverAll` → 곧바로 `ONGOING`, `pausedAt` null. (하네스가 두 대회를 나란히 못 세우므로 이 `it`은 **별도 `describe`**에서 새 하네스로 돈다 — `pause-resume.int-spec.ts`의 1-1 주석 참고.)

- [ ] **Step 2: 실패 확인**

Run: `npm run test:int -w backend -- syncing.int-spec`
Expected: FAIL — 1단계에서 status가 `ONGOING`, `completeSync` 없음

- [ ] **Step 3: `recoverAll`이 정지 시작 시각을 넘긴다**

`downtimeMs()`는 `resumePending.downMs`용으로 남기되, `recoverAll`이 하트비트 행을 한 번 읽어 둘 다 만든다:

```ts
      const beat = await this.prisma.serverHeartbeat.findUnique({ where: { id: 'singleton' } });
      const downtime = beat ? Math.max(0, Date.now() - beat.beatAt.getTime()) : null;
      // 정지가 시작된 시각. **소비 표시로 덮기 전에** 읽는다(아래 upsert).
      // 행이 없으면(최초 부팅) 지금 — 멈춘 시간이 0에서 시작한다.
      const pauseStartedAt = beat?.beatAt ?? new Date();
```

`recoverTournament(t.id, downtime ?? 0, pauseStartedAt)`.

- [ ] **Step 4: `recoverTournament`를 새 뜻으로**

1·2단계(다운타임만큼 `pausedMs` 증가, Redis 기준점 `+ downtime`)를 **지우고** 다음으로 바꾼다:

```ts
    // 1. 켠다(T96). **이미 SYNCING이면 pausedAt을 덮지 않는다** — 첫 정지 ·
    //    복구 대기 · 두 번째 정지가 `completeSync`의 한 번 보정에 들어간다.
    //    조건부라 ONGOING인 행만 바뀐다. 상태와 시각을 한 문장으로 쓴다.
    await this.prisma.tournament.updateMany({
      where: { id: tournamentId, status: TournamentStatus.ONGOING },
      data: { status: TournamentStatus.SYNCING, pausedAt: pauseStartedAt },
    });
    const t = await this.prisma.tournament.findUniqueOrThrow({
      where: { id: tournamentId },
      include: { blindStructure: true },
    });
    if (!t.startedAt) throw new Error('진행 중인데 startedAt이 없다');
    const pausedAtMs = t.pausedAt?.getTime() ?? null;

    // 2. 블라인드 기준점은 **대입**이다 — DB가 진실이다. 예전에는 부팅마다
    //    `+ downtime`으로 증분했고, 한 번 어긋나면 영영 어긋났다. 대입이면
    //    `completeSync`의 Redis 쓰기가 실패해도 다음 부팅이 고친다
    //    (`syncActivePlayer`가 증감이 아니라 대입인 것과 같은 이유).
    const blindBaseAt = t.startedAt.getTime() + t.pausedMs;
    const blind = await this.redis.getTournamentBlind(tournamentId);
    if (blind) {
      const { pausedAt: _stale, ...rest } = blind;
      await this.redis.setTournamentBlind(tournamentId, {
        ...rest,
        startedAt: blindBaseAt,
        ...(pausedAtMs !== null ? { pausedAt: pausedAtMs } : {}),
      });
      // 파생 캐시(`currentBlindLv`·`nextLevelAt`·`isBreak`)를 기준점에 맞춘다.
      await this.redis.checkAndSyncBlindLevel(tournamentId, { force: true });
      await this.redis.syncActivePlayer(tournamentId, t.activePlayers, t.startStack, t.entryFee);
    } else {
      const { dashboard, blindField, payoutTable } = buildTournamentMeta(t, blindBaseAt, pausedAtMs);
      await this.redis.setTournamentMeta(tournamentId, dashboard, blindField, payoutTable);
    }
```

(기존 주석 중 여전히 참인 것 — 인원수가 최후의 그물이라는 설명, 메타 유실 분기 설명 — 은 옮겨 남긴다. "정지 시간을 더한다"·"기준점만 민다" 설명은 새 뜻으로 고친다.)

3단계(테이블 루프)는 그대로. 루프 **뒤에**:

```ts
    // 앉은 테이블이 하나도 없으면 붙을 딜러가 없다. 접속 이벤트로 판정하면
    // 영영 안 풀리므로 여기서 한 번 센다(T96).
    const seatMaps = await this.redis.getTournamentTables(tournamentId);
    if (!seatMaps.some((m) => m.seatStatus.some(Boolean))) {
      await this.completeSync(tournamentId);
    }
```

- [ ] **Step 5: `completeSync`**

```ts
  /**
   * 그 대회의 딜러 태블릿이 전부 돌아왔다 — `SYNCING`을 끝내고 멈춘 시간만큼
   * 블라인드를 한 번 민다(T96). 게이트웨이가 n/n을 본 순간 부른다.
   *
   * **조건부 update가 문지기다.** 두 딜러가 동시에 n/n을 봐도 한 행만 바뀌고,
   * 진 쪽은 0행이라 아무것도 밀지 않는다. `pausedAt`까지 조건에 거는 이유는
   * 읽은 뒤 쓰기 전에 다른 부팅이 끼는 경우까지 막으려는 것이다.
   *
   * **Redis는 DB 뒤다** — 되돌아가지 않아서다. Redis 쓰기가 실패하면 시계가
   * 멈춘 채 남고, 다음 부팅이 DB에서 대입해 고친다(`recoverTournament` 2단계).
   *
   * 스냅샷을 쓰지 않는다 — `saveSnapshotUnlocked('boot-recovery')`의 근거
   * (호출자가 부팅 하나뿐)를 건드리지 않는다.
   *
   * @returns 이 호출이 끝냈으면 true
   */
  async completeSync(tournamentId: string): Promise<boolean> {
    const t = await this.prisma.tournament.findUnique({
      where: { id: tournamentId },
      select: { status: true, pausedAt: true, pausedMs: true, startedAt: true },
    });
    if (!t || t.status !== TournamentStatus.SYNCING || !t.pausedAt || !t.startedAt) return false;

    const delta = Math.max(0, Date.now() - t.pausedAt.getTime());
    const { count } = await this.prisma.tournament.updateMany({
      where: { id: tournamentId, status: TournamentStatus.SYNCING, pausedAt: t.pausedAt },
      data: { status: TournamentStatus.ONGOING, pausedAt: null, pausedMs: { increment: delta } },
    });
    if (count !== 1) return false;

    try {
      const blind = await this.redis.getTournamentBlind(tournamentId);
      if (blind) {
        const { pausedAt: _done, ...rest } = blind;
        await this.redis.setTournamentBlind(tournamentId, {
          ...rest,
          startedAt: t.startedAt.getTime() + t.pausedMs + delta,
        });
        await this.redis.checkAndSyncBlindLevel(tournamentId, { force: true });
      }
    } catch (e) {
      this.logger.error(`블라인드 재개 실패 — 다음 부팅이 DB로 고친다 (tournament=${tournamentId})`, e as Error);
    }

    this.logger.log(`SYNCING 종료 (tournament=${tournamentId}, 정지 ${delta}ms)`);
    return true;
  }
```

`recovery.module.ts`에 `exports: [RecoveryService]`.

- [ ] **Step 6: 보정 시점이 옮겨간 기존 검사를 새 뜻으로**

`recovery.service.int-spec.ts`에서 **의도는 그대로 두고 자리를 옮긴다.** 각 검사가 지키던 것:

| 기존 검사 | 새 뜻 |
|---|---|
| 하트비트 행이 없으면 정지 시간 보정을 건너뛴다 | 행이 없으면 `pausedAt` ≈ 지금, `pausedMs` 그대로 |
| 정지 시간을 누적한다 — 두 번 복구하면 합이 더해진다 | 두 번 복구해도 `pausedAt`은 첫 값. 누적은 `completeSync`가 한다(`pausedMs` 기존값 + Δ) |
| 복구가 하트비트를 소비한다 — 두 번 더하지 않는다 | 곧바로 다시 복구해도 `pausedAt`이 안 바뀌고, 두 번째의 `resumePending.downMs`가 작다 |
| 블라인드 기준점을 대회당 한 번만 민다 | `completeSync`가 테이블 셋이어도 Δ를 한 번 민다 |
| 블라인드 기준점을 밀면 레벨이 되돌아온다 · nextLevelAt도 같이 민다 · 과잉 보정으로 레벨이 한 칸 내려가면 캐시도 따라 내려간다 | 같은 입력을 **`completeSync` 뒤에** 본다(부팅 직후에는 `pausedAt`에서 멈춘 레벨) |
| 한 대회의 복구가 실패해도 다른 대회는 복구된다의 `pausedMs > 0` | 둘 다 `status === SYNCING` |
| blindField가 없으면 startedAt + pausedMs로 새로 세운다 | 그대로 + `blindField.pausedAt === t.pausedAt` |

`server-recovery.int-spec.ts`의 4단계(「블라인드 레벨이 정지 시간만큼 되돌아오고」)는 3단계 뒤에 `h.recovery.completeSync(h.tournamentId)`를 부른 다음 보게 바꾼다. `pause-resume.int-spec.ts`는 2단계 뒤에 `status === 'SYNCING'`을 보고, 4단계 앞에 `completeSync`를 부른다(재개가 n/n 뒤라는 새 순서).

- [ ] **Step 7: 통과 확인**

Run: `npm run test:int -w backend -- recovery syncing server-recovery pause-resume`
Expected: PASS 전부

- [ ] **Step 8: 되돌려 확인**

- `recoverAll` 조회를 `status: ONGOING`으로 되돌려 시나리오 3단계가 빨개지는지 → 복원.
- `completeSync`의 `where`에서 `status: SYNCING`을 빼 동시 n/n 검사가 빨개지는지(Δ 두 번) → 복원.
- 루프 뒤 n=0 분기를 지워 「앉은 테이블이 없는 대회」가 빨개지는지 → 복원.

- [ ] **Step 9: 커밋**

```bash
git add backend/src/recovery backend/src/scenario
git commit -m "feat(T96): 부팅이 SYNCING을 켜고 딜러 n/n이 끄며, 보정은 끄는 자리에서 한 번 한다"
```

---

### Task 4: 게이트웨이 — n/n 판정 · 진행 알림 · 딜러 명령 거절 (동시성: 태스크 리뷰 받음)

**Files:**
- Create: `backend/src/ws/sync-progress.ts` · `backend/src/ws/sync-progress.spec.ts`
- Create: `packages/contract/src/tournament-syncing.ts` · Modify: `packages/contract/src/index.ts`
- Modify: `backend/src/ws/ws.module.ts` (`imports`에 `RecoveryModule`)
- Modify: `backend/src/ws/ws.gateway.ts`
- Modify: `backend/src/ws/ws.gateway.int-spec.ts`

**Interfaces:**
- Consumes: `RecoveryService.completeSync`, `isLiveTournament`, `TournamentStatus.SYNCING`, `RedisService.getTournamentTables`.
- Produces: `syncProgress(required: string[], dealerTables: Iterable<string>): { present: number; required: number; done: boolean }`. contract `TournamentSyncingSchema = { syncing: boolean, present: int≥0, required: int≥0 }`, 이벤트 이름 `'tournamentSyncing'`. 게이트웨이 `reportSync(tournamentId)`(private).

- [ ] **Step 1: 순수 판정의 실패하는 테스트**

`sync-progress.spec.ts`:

```ts
import { syncProgress } from './sync-progress';

it('앉은 테이블 전부에 딜러가 있으면 done', () => {
  expect(syncProgress(['a', 'b'], ['a', 'b'])).toEqual({ present: 2, required: 2, done: true });
});
it('하나라도 없으면 아직이다', () => {
  expect(syncProgress(['a', 'b'], ['a'])).toEqual({ present: 1, required: 2, done: false });
});
/** **반대 입력.** 셈 밖 테이블의 딜러가 k를 부풀리면 안 된다. */
it('앉은 사람이 없는 테이블의 딜러는 세지 않는다', () => {
  expect(syncProgress(['a'], ['a', 'z'])).toEqual({ present: 1, required: 1, done: true });
  expect(syncProgress(['a', 'b'], ['a', 'z'])).toEqual({ present: 1, required: 2, done: false });
});
it('한 테이블에 딜러 소켓이 둘이어도 한 번 센다', () => {
  expect(syncProgress(['a', 'b'], ['a', 'a'])).toEqual({ present: 1, required: 2, done: false });
});
it('앉은 테이블이 없으면 done', () => {
  expect(syncProgress([], [])).toEqual({ present: 0, required: 0, done: true });
});
```

Run: `npm test -w backend -- sync-progress` → FAIL(모듈 없음)

- [ ] **Step 2: 구현**

`sync-progress.ts`:

```ts
/**
 * 딜러 복귀 k/n(T96). 순수 함수 — 소켓도 Redis도 모른다.
 *
 * **n은 앉은 사람이 있는 테이블이다.** 빈 테이블에는 딜러가 없을 수 있어, 넣으면
 * 영영 안 찬다. **k는 그 n 안에서만 센다** — 빈 테이블의 딜러가 k를 부풀리면
 * 멈춘 테이블이 남았는데 n/n이 된다.
 */
export function syncProgress(required: string[], dealerTables: Iterable<string>) {
  const need = new Set(required);
  const present = new Set([...dealerTables].filter((id) => need.has(id))).size;
  return { present, required: need.size, done: present === need.size };
}
```

Run → PASS.

- [ ] **Step 3: contract 이벤트**

`packages/contract/src/tournament-syncing.ts`:

```ts
import { z } from "zod";

/**
 * 서버 복구 중 딜러 복귀 진행(`tournamentSyncing`, T96). 그 대회의 딜러에게만 간다.
 *
 * **스냅샷 필드가 아니라 별도 이벤트다.** 대회 단위 정보라 테이블 스냅샷에
 * 실으면 테이블마다 같은 값을 들고 어긋날 수 있다(`tournamentClosed`와 같은 판단).
 *
 * `syncing: false`는 **서버가 `SYNCING`을 끝냈다**는 뜻이다 — `present === required`만
 * 보고 버튼을 열면, 서버가 아직 끝내지 못한 순간에 누른 재개가 거절된다.
 */
export const TournamentSyncingSchema = z.object({
  syncing: z.boolean(),
  present: z.int().min(0),
  required: z.int().min(0),
});
export type TournamentSyncing = z.infer<typeof TournamentSyncingSchema>;
export const TOURNAMENT_SYNCING_EVENT = "tournamentSyncing" as const;
```

`index.ts`에 export.

- [ ] **Step 4: 게이트웨이 통합의 실패하는 테스트**

`ws.gateway.int-spec.ts`의 게이트웨이 생성부에 `RecoveryService` 자리를 더한다(이 파일은 `new WsGateway(...)`로 세운다 — 생성자 인자 순서에 맞춰 `recovery` 목을 넣는다: `{ completeSync: jest.fn().mockResolvedValue(true) }`). 새 `describe('SYNCING (T96)')`:

- 대회를 `SYNCING`으로(`prisma.tournament.update({ status: 'SYNCING', pausedAt: new Date() })`), 좌석 비트맵에 `TABLE`·`OTHER_TABLE` 둘 다 한 명씩(이 파일의 비트맵 준비 코드 또는 `redis.rebuildSeatBitmap`).
- `TABLE` 딜러 접속 → 그 소켓이 `tournamentSyncing {syncing:true,present:1,required:2}`를 받는다. `completeSync` 안 불림.
- `OTHER_TABLE` 딜러 접속 → `completeSync(TOURNAMENT)` 불림, 두 딜러 소켓이 `{syncing:false,present:2,required:2}`를 받는다.
- **딜러 명령 거절**: `SYNCING`인 채로 `handleDealerAction(dealerClient, { action: 'START_PRE_FLOP' })` → `{ event:'error', data:'딜러가 모두 돌아올 때까지 기다려 주세요.' }`, `dealer.startPreFlop` 안 불림.
- **반대 입력**: 대회가 `ONGOING`이면 딜러 접속에 `tournamentSyncing`을 안 보내고, 같은 명령이 `dealer.startPreFlop`까지 간다.
- **좌석 소켓은 k에 안 든다**: `SYNCING`에서 `OTHER_TABLE`에 좌석 소켓만 붙은 상태로 `TABLE` 딜러 접속 → `present:1`.
- 딜러 소켓이 끊기면(`handleDisconnect`) 남은 딜러가 `present`가 줄어든 알림을 받는다.

Run: `npm run test:int -w backend -- ws.gateway.int-spec -t "SYNCING"` → FAIL

- [ ] **Step 5: 게이트웨이 구현**

`ws.module.ts` `imports`에 `RecoveryModule`. 게이트웨이 생성자에 `private readonly recovery: RecoveryService`.

```ts
  /**
   * 그 대회가 `SYNCING`이면 딜러 복귀를 다시 세어 딜러들에게 알리고, n/n이면
   * 끝낸다(T96).
   *
   * **소켓 수를 판정에 쓴다.** T95가 피했던 것은 게이트웨이에 하트비트가 없어
   * 좀비가 살아 보였기 때문이고, 그 전제는 `sweepSockets`가 없앴다. 그래도
   * 좀비가 끼면 n/n이 조금 일찍 풀릴 뿐이다 — 판을 여는 것은 여전히 테이블마다
   * 딜러가 누르는 재개다.
   *
   * 프로세스가 하나라(B9) 메모리 안에서 세도 일관된다.
   */
  private async reportSync(tournamentId: string) {
    const t = await this.prisma.tournament.findUnique({ where: { id: tournamentId }, select: { status: true } });
    if (t?.status !== TournamentStatus.SYNCING) return;

    const seatMaps = await this.redis.getTournamentTables(tournamentId);
    const required = seatMaps.filter((m) => m.seatStatus.some(Boolean)).map((m) => m.tableId);
    const dealerTables = required.filter((id) =>
      [...(this.tableSessions.get(id) ?? [])].some(
        (s: any) => s.role === Role.DEALER && s.readyState === WebSocket.OPEN,
      ),
    );
    const progress = syncProgress(required, dealerTables);

    let syncing = true;
    if (progress.done) {
      // 진 쪽(동시 n/n)은 false다. 이긴 쪽이 알린다.
      if (!(await this.recovery.completeSync(tournamentId))) return;
      syncing = false;
    }

    const payload = TournamentSyncingSchema.parse({ syncing, present: progress.present, required: progress.required });
    for (const id of required) {
      for (const s of this.tableSessions.get(id) ?? []) {
        if ((s as any).role !== Role.DEALER || s.readyState !== WebSocket.OPEN) continue;
        try { s.send(JSON.stringify({ event: TOURNAMENT_SYNCING_EVENT, data: payload })); } catch { /* 다음 틱이 치운다 */ }
      }
    }
  }
```

- `handleConnection`의 테이블 분기 끝(`renderGame` 전송 뒤): `if (payload.role === Role.DEALER && payload.tournamentId) await this.reportSync(payload.tournamentId);`
- `handleDisconnect` 끝: 끊긴 소켓이 딜러면 `void this.reportSync(tournamentId).catch((e) => this.logger.error('SYNCING 재집계 실패', e));` (`handleDisconnect`는 동기다).
- `runDealerAction` 맨 앞:

```ts
    // **복구 중에는 딜러 명령을 전부 받지 않는다**(T96). 핸드 시작은 깜깜한
    // 좌석을 판에 넣고(30초 뒤 자동 폴드), 승자 입력은 리바인 창(15초)을 꺼진
    // 태블릿으로 보낸다. 재개는 딜러가 다 돌아온 뒤라야 뜻이 있다.
    const t = await this.prisma.tournament.findUnique({ where: { id: tournamentId }, select: { status: true } });
    if (t?.status === TournamentStatus.SYNCING) {
      throw new Error('딜러가 모두 돌아올 때까지 기다려 주세요.');
    }
```

(`TournamentStatus` import 추가. `runDealerAction`이 던지면 `handleDealerAction`의 `catch`가 `{event:'error'}`로 돌려준다.)

- [ ] **Step 6: 통과 · 되돌려 확인**

Run: `npm test -w backend -- sync-progress && npm run test:int -w backend -- ws.gateway.int-spec`
Expected: PASS. 그다음 `runDealerAction`의 가드를 지워 거절 검사가, `syncProgress`의 `need.has` 거르기를 지워 반대 입력 검사가 빨개지는지 확인 → 복원.

- [ ] **Step 7: 커밋**

```bash
git add packages/contract/src/tournament-syncing.ts packages/contract/src/index.ts backend/src/ws
git commit -m "feat(T96): 게이트웨이가 딜러 복귀를 세어 SYNCING을 끝내고, 그 전에는 딜러 명령을 받지 않는다"
```

---

### Task 5: 화면

**Files:**
- Modify: `frontend/src/app/(player)/me/page.tsx` · `page.test.tsx`
- Modify: `frontend/src/app/(player)/tournaments/[id]/page.tsx` · `page.test.tsx`
- Modify: `frontend/src/app/(console)/stores/[storeId]/tournaments/[tournamentId]/ConsoleClient.tsx` · `ConsoleClient.test.tsx`
- Modify: `frontend/src/app/(board)/stores/[storeId]/tournaments/[tournamentId]/display/DisplayClient.tsx` (+ 테스트 파일이 있으면 거기, 없으면 `DisplayClient.test.tsx` 생성)
- Modify: `frontend/src/app/(terminal)/dealer/table/[tableId]/DealerGameClient.tsx` · `DealerGameClient.test.tsx`

**Interfaces:**
- Consumes: contract `TournamentStatus`, `TournamentStatusSchema`, `BlindField.pausedAt`, `TournamentSyncingSchema`, `TOURNAMENT_SYNCING_EVENT`.

- [ ] **Step 1: 실패하는 테스트 (파일마다 기존 픽스처 모양으로)**

- `me/page.test.tsx`: `tournament.status: 'SYNCING'`인 참가 행 → 「진행 중」 섹션에 있고 「복구 중」이 보인다, 「종료」가 안 보인다, OTP 버튼이 살아 있다.
- `tournaments/[id]/page.test.tsx`: `status: 'SYNCING'` → 「복구 중」이 보이고, 등록이 열려 있으면 참가 버튼은 살아 있다(결제는 `SYNCING`을 막지 않는다).
- `ConsoleClient.test.tsx`: `status: 'SYNCING'` → 배지 「복구 중」, 마무리 영역(「대회 마무리 — 되돌릴 수 없습니다」)이 보인다.
- `DisplayClient` 테스트: `blindField.pausedAt`이 있는 응답 → 「서버 복구 중」이 보이고, 남은 시간이 `nextLevelAt - pausedAt`로 고정(1초 뒤에도 같은 글자).
- `DealerGameClient.test.tsx`: `resumePending`이 있는 스냅샷 + `tournamentSyncing {syncing:true,present:7,required:9}` → 「딜러 7/9 복귀」가 보이고 「이어서 진행」이 `disabled`. 이어서 `{syncing:false,present:9,required:9}` → 버튼이 활성.

Run: `npm test -w frontend` → 새 검사들 FAIL

- [ ] **Step 2: 구현**

- `me/page.tsx`: `statusLabel` 함수를 지우고

```ts
// `Record`라 계약에 상태가 늘면 여기서 컴파일 에러가 난다. 함수로 가르던
// 시절에는 새 상태(`SYNCING`)가 폴백 「종료」로 조용히 떨어졌다(T96).
const STATUS_LABEL: Record<TournamentStatus, string> = {
  PENDING: '시작 전',
  ONGOING: '진행 중',
  SYNCING: '복구 중',
  FINISHED: '종료',
  CANCELLED: '취소',
};
```
  `Participation.tournament.status`의 타입을 `TournamentStatus`로. 쓰는 자리는 `STATUS_LABEL[row.tournament.status]`.
- `tournaments/[id]/page.tsx`: `TournamentDetail.status`를 `TournamentStatus`로. 상태 줄에서 닫힌 상태가 아니고 `status === 'SYNCING'`이면 「복구 중 · 등록 열림/마감」처럼 앞에 붙인다(색은 `--ink-subtle`).
- `ConsoleClient.tsx`: `STATUS_LABEL`을 `Record<TournamentStatus, string>`으로 바꾸고 `SYNCING: '복구 중'`. 마무리 영역 조건 `tournament.status === 'ONGOING'` → `(tournament.status === 'ONGOING' || tournament.status === 'SYNCING')`에 주석(「시작 뒤의 대회. 복구 중에도 중단은 열려 있어야 한다」).
- `DisplayClient.tsx`: `isBreak` 분기 **앞에**

```tsx
  // 서버 복구 중(T96). 시계가 `pausedAt`에서 멈춰 있다 — 벽시계로 깎으면
  // 태블릿들이 돌아오는 동안 전광판만 레벨을 넘긴다.
  if (blindField.pausedAt !== undefined) {
    return (
      <div className="flex h-screen flex-col items-center justify-center bg-sb-bg px-8 text-center">
        <div className="font-cond text-[clamp(20px,3vw,30px)] uppercase tracking-[0.3em] text-sb-dim">서버 복구 중</div>
        <div className="font-cond text-[clamp(70px,15vw,170px)] font-bold leading-[0.9] tabular-nums text-sb-dim">
          {formatClock(blindField.nextLevelAt - blindField.pausedAt)}
        </div>
        <div className="mt-5 font-cond text-[clamp(17px,2.4vw,26px)] tracking-[0.14em] text-sb-dim">
          테이블이 돌아오면 이어서 진행합니다
        </div>
      </div>
    );
  }
```
- `DealerGameClient.tsx`: 상태 `const [sync, setSync] = useState<{ present: number; required: number } | null>(null);`. `onMessage`에 `TOURNAMENT_SYNCING_EVENT` 분기(`TournamentSyncingSchema.safeParse` → `setSync(p.data.syncing ? { present, required } : null)`; 실패는 `tournamentClosed` 분기처럼 `console.error` 후 무시). 정지 배너(`resumePending &&`) 안에 `sync`가 있으면 「딜러 {present}/{required} 복귀 — 전원이 돌아오면 이어서 진행할 수 있습니다」를 적고 「이어서 진행」 버튼에 `disabled={sync !== null}`.

- [ ] **Step 3: 통과 확인**

Run: `npm run typecheck && npm test -w frontend`
Expected: PASS 전부

- [ ] **Step 4: 되돌려 확인**

`STATUS_LABEL`에서 `SYNCING` 키를 지워 타입 체크가 빨개지는지(컴파일 에러가 곧 검사다), 딜러 버튼의 `disabled`를 지워 딜러 검사가 빨개지는지 확인 → 복원.

- [ ] **Step 5: 커밋**

```bash
git add frontend/src
git commit -m "feat(T96): 참가자 폰 · 전광판 · 콘솔 · 딜러 화면이 복구 중을 보여준다"
```

---

### Task 6 (메인): 기준선과 SSOT

하위 에이전트 몫이 아니다(`CLAUDE.md`의 「문서는 메인이 맡는다」).

- [ ] 최종 전체 리뷰(opus, 자르지 않는다)
- [ ] 이 브랜치를 최신 `main`(PR 1 머지 뒤) 위로 맞추고 기준선 실측: `npm run typecheck`, `npm run test`, `npm run test:int`, `cd load && npm test`
- [ ] PR 생성 → 번호 확인
- [ ] SSOT 커밋: `tickets-audit.md`(T96 `완료 (#번호)`, 잔여 목록 「12,000명 재실측」을 닫고 근거 — T76이 원인과 수정을 확정했다 — 를 적는다), `domain.md`(「서버가 죽어도 대회는 계속된다」 표의 시간 보정 시점이 부팅 → n/n, 「중단은 정지가 아니다」에 `SYNCING`은 사람이 거는 정지가 아니라 복구라는 줄), `CLAUDE.md` 기준선
- [ ] 머지, `git fetch --prune`, 로컬 브랜치 정리
