# 앱 셸 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 프론트엔드에 테스트 기반과 목업 API 계층을 세우고, 명세의 네 개 면을 담을 라우트 그룹과 역할 가드를 만든다.

**Architecture:** Next.js App Router의 라우트 그룹으로 면을 나눈다. 그룹마다 레이아웃이 분리되므로 콘솔의 사이드바와 단말의 전체화면이 자연스럽게 갈린다. 백엔드는 아직 명세대로 구현돼 있지 않으므로 MSW가 네트워크 계층에서 요청을 가로채 픽스처를 돌려준다. 프론트는 명세상의 진짜 엔드포인트로 fetch하고, 백엔드가 따라오면 핸들러만 지운다.

**Tech Stack:** Next.js 16.1.6, React 19.2.3, TypeScript 5, Tailwind 4, Vitest 3, Testing Library, MSW 2

## Global Constraints

- 모노레포 루트는 `C:\KGW\GITHUB\playsync-V2`. 프론트 워크스페이스는 `frontend/`.
- 명령은 루트에서 `-w frontend`로 실행한다.
- `@/*`는 `frontend/src/*`로 해석된다 (`frontend/tsconfig.json`에 이미 설정됨).
- 코드 주석과 커밋 메시지는 한국어로 쓴다.
- 프론트의 역할 가드는 UX용이며 보안 경계가 아니다. 실제 권한 판정은 백엔드가 한다.
- JWT 페이로드는 `{ sub, nickname, role }`이다. `role`은 `USER` / `DEALER` / `STORE_ADMIN` / `PLATFORM_ADMIN` 중 하나다.
- 목업 핸들러가 없는 요청은 테스트에서 에러가 나야 한다. 조용히 통과시키지 않는다.
- **마크업은 디자인하지 않는다.** 스타일 클래스를 붙이지 말고 구조와 시맨틱만 둔다. 이 계획의 관심사는 서버 액션·가드·데이터 흐름 같은 로직이다.

---

### Task 1: 테스트 기반과 세션 유틸

프론트에는 테스트 러너가 없다. 이후 모든 태스크가 이것 위에 선다.

테스트 기반이 도는지는 실제로 쓸 코드로 증명한다. 기반 검증만을 위한 모듈을 만들면 영구히 남는 죽은 코드가 된다. 세션 유틸은 순수 함수라 첫 대상으로 알맞고, 미들웨어가 바로 이어 쓴다.

**Files:**
- Create: `frontend/vitest.config.mts`
- Create: `frontend/vitest.setup.ts`
- Create: `frontend/src/lib/session.ts`
- Create: `frontend/src/lib/session.test.ts`
- Modify: `frontend/package.json`
- Modify: `package.json` (루트)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `npm run test -w frontend` 명령. 모든 후속 태스크가 이 명령으로 테스트를 돌린다
  - `type Role = 'USER' | 'DEALER' | 'STORE_ADMIN' | 'PLATFORM_ADMIN'`
  - `type Session = { userId: string; nickname: string; role: Role }`
  - `decodeSession(token: string | undefined): Session | null`
  - `SESSION_COOKIE = 'accessToken'`

- [ ] **Step 1: 의존성 설치**

```bash
npm install -D -w frontend vitest@^3 @vitejs/plugin-react@^5 jsdom@^26 @testing-library/react@^16 @testing-library/dom@^10 @testing-library/jest-dom@^6 vite-tsconfig-paths@^5
```

- [ ] **Step 2: vitest 설정 작성**

`frontend/vitest.config.mts`:

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import tsconfigPaths from 'vite-tsconfig-paths';

export default defineConfig({
  plugins: [tsconfigPaths(), react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./vitest.setup.ts'],
    globals: true,
    include: ['src/**/*.test.{ts,tsx}'],
  },
});
```

- [ ] **Step 3: 셋업 파일 작성**

`frontend/vitest.setup.ts`:

```typescript
import '@testing-library/jest-dom/vitest';

// 테스트 환경에서는 상대 경로 fetch가 불가능하다. 오리진을 고정해 두고
// apiUrl()이 이 값을 앞에 붙인다.
process.env.NEXT_PUBLIC_API_BASE = 'http://localhost:3000';
```

- [ ] **Step 4: 실패하는 테스트 작성**

`frontend/src/lib/session.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { decodeSession } from '@/lib/session';

function makeToken(payload: Record<string, unknown>): string {
  const encode = (obj: unknown) =>
    Buffer.from(JSON.stringify(obj)).toString('base64url');
  return `${encode({ alg: 'HS256', typ: 'JWT' })}.${encode(payload)}.sig`;
}

describe('decodeSession', () => {
  it('페이로드에서 세션을 뽑는다', () => {
    const token = makeToken({
      sub: 'user-1',
      nickname: '건우',
      role: 'STORE_ADMIN',
    });

    expect(decodeSession(token)).toEqual({
      userId: 'user-1',
      nickname: '건우',
      role: 'STORE_ADMIN',
    });
  });

  it('토큰이 없으면 null', () => {
    expect(decodeSession(undefined)).toBeNull();
  });

  it('형식이 깨진 토큰은 null', () => {
    expect(decodeSession('망가진문자열')).toBeNull();
  });

  it('페이로드가 base64가 아니면 null', () => {
    expect(decodeSession('a.!!!.c')).toBeNull();
  });

  it('모르는 역할은 null', () => {
    const token = makeToken({ sub: 'u', nickname: 'n', role: 'GOD' });
    expect(decodeSession(token)).toBeNull();
  });

  it('필수 필드가 빠지면 null', () => {
    const token = makeToken({ sub: 'u', role: 'USER' });
    expect(decodeSession(token)).toBeNull();
  });
});
```

첫 케이스의 닉네임을 한글로 둔 것은 의도적이다. UTF-8 디코딩 경로를 지나간다.

- [ ] **Step 5: 테스트가 실패하는지 확인**

`frontend/package.json`의 `scripts`에 추가:

```json
"test": "vitest run"
```

실행: `npm run test -w frontend`

예상: `Failed to resolve import "@/lib/session"` 로 실패

- [ ] **Step 6: 구현**

`frontend/src/lib/session.ts`:

```typescript
export const SESSION_COOKIE = 'accessToken';

export const ROLES = [
  'USER',
  'DEALER',
  'STORE_ADMIN',
  'PLATFORM_ADMIN',
] as const;

export type Role = (typeof ROLES)[number];

export type Session = {
  userId: string;
  nickname: string;
  role: Role;
};

function isRole(value: unknown): value is Role {
  return typeof value === 'string' && (ROLES as readonly string[]).includes(value);
}

// 이 함수는 미들웨어에서도 불린다. 미들웨어는 Edge 런타임에서 도는데 거기엔
// Buffer가 없으므로 atob으로 푼다. base64url은 문자 두 개가 다르고 패딩이
// 없으니 표준 base64로 되돌린 뒤 넘긴다.
function decodeBase64Url(input: string): string {
  const base64 = input.replace(/-/g, '+').replace(/_/g, '/');
  const padded = base64.padEnd(Math.ceil(base64.length / 4) * 4, '=');
  const binary = atob(padded);

  const bytes = Uint8Array.from(binary, (ch) => ch.charCodeAt(0));
  return new TextDecoder().decode(bytes);
}

// 서명은 검증하지 않는다. 프론트의 역할 판정은 어떤 화면을 보여줄지 정하는
// 데만 쓰고, 실제 권한은 백엔드가 판정한다. 여기서 검증하는 시늉을 하면
// 그것이 보안 경계인 것처럼 오해를 부른다.
export function decodeSession(token: string | undefined): Session | null {
  if (!token) return null;

  const parts = token.split('.');
  if (parts.length !== 3) return null;

  let payload: unknown;
  try {
    payload = JSON.parse(decodeBase64Url(parts[1]));
  } catch {
    return null;
  }

  if (typeof payload !== 'object' || payload === null) return null;

  const { sub, nickname, role } = payload as Record<string, unknown>;

  if (typeof sub !== 'string' || sub === '') return null;
  if (typeof nickname !== 'string' || nickname === '') return null;
  if (!isRole(role)) return null;

  return { userId: sub, nickname, role };
}
```

- [ ] **Step 7: 테스트 통과 확인**

실행: `npm run test -w frontend`

예상: `6 passed`

- [ ] **Step 8: 루트 test 스크립트에 연결**

루트 `package.json`의 `test`를 수정:

```json
"test": "npm run test -w @playsync/contract && npm run test -w backend && npm run test -w frontend"
```

- [ ] **Step 9: 전체 테스트 확인**

실행: `npm run test`

예상: contract 41 passed, backend 90 passed, frontend 6 passed

- [ ] **Step 10: 커밋**

```bash
git add frontend/vitest.config.mts frontend/vitest.setup.ts frontend/src/lib/session.ts frontend/src/lib/session.test.ts frontend/package.json package.json package-lock.json
git commit -m "test: 프론트엔드 테스트 기반과 세션 유틸"
```

---

### Task 2: 목업 API 계층

백엔드가 명세대로 구현돼 있지 않다. MSW가 네트워크 계층에서 가로챈다. 프론트 코드는 진짜 엔드포인트로 fetch하므로, 백엔드가 따라오면 핸들러만 지우면 된다.

**Files:**
- Create: `frontend/src/lib/api.ts`
- Create: `frontend/src/lib/api.test.ts`
- Create: `frontend/src/mocks/handlers.ts`
- Create: `frontend/src/mocks/server.ts`
- Modify: `frontend/vitest.setup.ts`

**Interfaces:**
- Consumes: Task 1의 `npm run test -w frontend`
- Produces:
  - `apiUrl(path: string): string` — 상대 경로를 오리진 포함 URL로 만든다
  - `apiFetch(path: string, init?: RequestInit): Promise<Response>` — `apiUrl`을 적용하고 쿠키를 함께 보낸다
  - `handlers: RequestHandler[]` — MSW 핸들러 배열. 후속 태스크가 여기에 핸들러를 추가한다

- [ ] **Step 1: MSW 설치**

```bash
npm install -D -w frontend msw@^2
```

- [ ] **Step 2: 실패하는 테스트 작성**

`frontend/src/lib/api.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { apiUrl, apiFetch } from '@/lib/api';

describe('apiUrl', () => {
  it('설정된 오리진을 앞에 붙인다', () => {
    expect(apiUrl('/api/auth/login')).toBe('http://localhost:3000/api/auth/login');
  });
});

describe('apiFetch', () => {
  it('목업 핸들러가 응답한다', async () => {
    const res = await apiFetch('/api/auth/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ nickname: 'tester', password: 'pw' }),
    });

    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.accessToken).toEqual(expect.any(String));
  });

  it('핸들러가 없는 경로는 에러가 된다', async () => {
    await expect(apiFetch('/api/없는경로')).rejects.toThrow();
  });
});
```

- [ ] **Step 3: 테스트가 실패하는지 확인**

실행: `npm run test -w frontend`

예상: `Failed to resolve import "@/lib/api"` 로 실패

- [ ] **Step 4: api 유틸 구현**

`frontend/src/lib/api.ts`:

```typescript
// 브라우저에서는 빈 문자열이라 상대 경로 그대로 나가고, next.config.ts의
// rewrite가 백엔드로 넘긴다. 테스트 환경에서는 상대 경로 fetch가 불가능하므로
// vitest.setup.ts가 오리진을 채워 준다.
export function apiUrl(path: string): string {
  const base = process.env.NEXT_PUBLIC_API_BASE ?? '';
  return `${base}${path}`;
}

export function apiFetch(path: string, init?: RequestInit): Promise<Response> {
  return fetch(apiUrl(path), {
    credentials: 'include',
    ...init,
  });
}
```

- [ ] **Step 5: 목업 핸들러 작성**

`frontend/src/mocks/handlers.ts`:

```typescript
import { http, HttpResponse } from 'msw';

// 목업 JWT. 서명은 아무 값이나 넣는다 — 프론트는 페이로드만 읽고, 검증은
// 백엔드 몫이다.
function fakeToken(payload: Record<string, unknown>): string {
  const encode = (obj: unknown) =>
    Buffer.from(JSON.stringify(obj)).toString('base64url');
  return `${encode({ alg: 'HS256', typ: 'JWT' })}.${encode(payload)}.mock`;
}

// 경로 앞의 `*`는 상대 경로와 절대 경로를 모두 잡는다.
export const handlers = [
  http.post('*/api/auth/login', async ({ request }) => {
    const { nickname } = (await request.json()) as { nickname: string };

    return HttpResponse.json({
      accessToken: fakeToken({
        sub: 'user-1',
        nickname,
        role: 'USER',
      }),
    });
  }),
];
```

- [ ] **Step 6: 테스트용 MSW 서버 작성**

`frontend/src/mocks/server.ts`:

```typescript
import { setupServer } from 'msw/node';
import { handlers } from '@/mocks/handlers';

export const server = setupServer(...handlers);
```

- [ ] **Step 7: 셋업 파일에 서버 연결**

`frontend/vitest.setup.ts` 전체를 다음으로 교체:

```typescript
import '@testing-library/jest-dom/vitest';
import { beforeAll, afterEach, afterAll } from 'vitest';
import { server } from '@/mocks/server';

// 테스트 환경에서는 상대 경로 fetch가 불가능하다. 오리진을 고정해 두고
// apiUrl()이 이 값을 앞에 붙인다.
process.env.NEXT_PUBLIC_API_BASE = 'http://localhost:3000';

// 핸들러 없는 요청을 통과시키면 목업이 빠진 것을 못 알아챈다. 에러로 세운다.
beforeAll(() => server.listen({ onUnhandledRequest: 'error' }));
afterEach(() => server.resetHandlers());
afterAll(() => server.close());
```

- [ ] **Step 8: 테스트 통과 확인**

실행: `npm run test -w frontend`

예상: `9 passed`

- [ ] **Step 9: 커밋**

```bash
git add frontend/src/lib/api.ts frontend/src/lib/api.test.ts frontend/src/mocks/ frontend/vitest.setup.ts frontend/package.json package-lock.json
git commit -m "feat: MSW 목업 API 계층 추가"
```

---

### Task 3: 역할 가드 미들웨어

**Files:**
- Create: `frontend/src/middleware.ts`
- Create: `frontend/src/middleware.test.ts`

**Interfaces:**
- Consumes: Task 1의 `decodeSession`, `SESSION_COOKIE`, `Role`
- Produces: `middleware(request: NextRequest): NextResponse`

가드 규칙:

| 경로 | 요구 |
|---|---|
| `/login`, `/register` | 없음 |
| `/table/*`, `/dealer/*` | 없음 (OTP로 진입한다) |
| `/admin/*` | `PLATFORM_ADMIN` |
| `/stores/*` | `STORE_ADMIN` 또는 `PLATFORM_ADMIN` |
| 그 외 | 로그인만 되어 있으면 통과 |

미인증은 `/login?next=<경로>`로 보낸다. 역할이 안 맞으면 404를 낸다 — 403은 그 상점이 존재한다는 사실을 알려준다.

- [ ] **Step 1: 실패하는 테스트 작성**

`frontend/src/middleware.test.ts`:

```typescript
// @vitest-environment node
import { describe, it, expect } from 'vitest';
import { NextRequest } from 'next/server';
import { middleware } from '@/middleware';
import { SESSION_COOKIE, type Role } from '@/lib/session';

function makeToken(role: Role): string {
  const encode = (obj: unknown) =>
    Buffer.from(JSON.stringify(obj)).toString('base64url');
  return `${encode({ alg: 'HS256' })}.${encode({
    sub: 'u1',
    nickname: 'n',
    role,
  })}.sig`;
}

function request(path: string, role?: Role): NextRequest {
  const req = new NextRequest(new URL(`http://localhost:3000${path}`));
  if (role) req.cookies.set(SESSION_COOKIE, makeToken(role));
  return req;
}

describe('미인증', () => {
  it('보호된 경로는 로그인으로 보낸다', () => {
    const res = middleware(request('/stores/s1'));

    expect(res.status).toBe(307);
    const location = new URL(res.headers.get('location')!);
    expect(location.pathname).toBe('/login');
    expect(location.searchParams.get('next')).toBe('/stores/s1');
  });

  it('로그인 페이지는 통과시킨다', () => {
    expect(middleware(request('/login')).status).toBe(200);
  });

  it('좌석 태블릿은 통과시킨다', () => {
    expect(middleware(request('/table/t1')).status).toBe(200);
  });

  it('딜러 단말은 통과시킨다', () => {
    expect(middleware(request('/dealer/d1')).status).toBe(200);
  });
});

describe('역할 가드', () => {
  it('일반 유저는 어드민에 접근하면 404', () => {
    expect(middleware(request('/admin', 'USER')).status).toBe(404);
  });

  it('상점주는 어드민에 접근하면 404', () => {
    expect(middleware(request('/admin', 'STORE_ADMIN')).status).toBe(404);
  });

  it('플랫폼 운영자는 어드민을 통과한다', () => {
    expect(middleware(request('/admin', 'PLATFORM_ADMIN')).status).toBe(200);
  });

  it('일반 유저는 상점 콘솔에 접근하면 404', () => {
    expect(middleware(request('/stores/s1', 'USER')).status).toBe(404);
  });

  it('상점주는 상점 콘솔을 통과한다', () => {
    expect(middleware(request('/stores/s1', 'STORE_ADMIN')).status).toBe(200);
  });

  it('플랫폼 운영자도 상점 콘솔을 통과한다', () => {
    expect(middleware(request('/stores/s1', 'PLATFORM_ADMIN')).status).toBe(200);
  });

  it('일반 유저는 모바일 화면을 통과한다', () => {
    expect(middleware(request('/tournaments', 'USER')).status).toBe(200);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

실행: `npm run test -w frontend`

예상: `Failed to resolve import "@/middleware"` 로 실패

- [ ] **Step 3: 구현**

`frontend/src/middleware.ts`:

```typescript
import { NextResponse, type NextRequest } from 'next/server';
import { decodeSession, SESSION_COOKIE, type Role } from '@/lib/session';

// 로그인 없이 열리는 경로. 단말은 사용자 로그인이 아니라 OTP로 진입한다.
const PUBLIC_PREFIXES = ['/login', '/register', '/table', '/dealer'];

// 경로별로 요구하는 역할. 앞에서부터 처음 걸리는 것이 적용된다.
const ROLE_RULES: { prefix: string; allow: readonly Role[] }[] = [
  { prefix: '/admin', allow: ['PLATFORM_ADMIN'] },
  { prefix: '/stores', allow: ['STORE_ADMIN', 'PLATFORM_ADMIN'] },
];

function startsWithSegment(pathname: string, prefix: string): boolean {
  return pathname === prefix || pathname.startsWith(`${prefix}/`);
}

export function middleware(request: NextRequest): NextResponse {
  const { pathname } = request.nextUrl;

  if (PUBLIC_PREFIXES.some((p) => startsWithSegment(pathname, p))) {
    return NextResponse.next();
  }

  const session = decodeSession(request.cookies.get(SESSION_COOKIE)?.value);

  if (!session) {
    const loginUrl = new URL('/login', request.url);
    loginUrl.searchParams.set('next', pathname);
    return NextResponse.redirect(loginUrl);
  }

  const rule = ROLE_RULES.find((r) => startsWithSegment(pathname, r.prefix));

  // 역할이 맞지 않으면 404다. 403은 그 자원이 존재한다는 사실을 알려준다.
  if (rule && !rule.allow.includes(session.role)) {
    return new NextResponse(null, { status: 404 });
  }

  return NextResponse.next();
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
};
```

- [ ] **Step 4: 테스트 통과 확인**

실행: `npm run test -w frontend`

예상: `20 passed`

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/middleware.ts frontend/src/middleware.test.ts
git commit -m "feat: 역할 기반 라우트 가드 미들웨어"
```

---

### Task 4: 라우트 그룹과 레이아웃

명세의 세 그룹을 만들고, 기존 페이지 중 **건질 것만** 옮긴다. 페이지 내용은 이 태스크에서 바꾸지 않는다 — 구조만 세운다.

기존 코드는 MVP다. 실제 로직이 들어 있는 것만 남기고 나머지는 지운다. 후속 계획이 어차피 새로 쓸 화면을 끌고 가면 죽은 코드와 깨지는 import만 남는다.

| 남긴다 | 이유 |
|---|---|
| `playsync/[tableId]/` | `PokerTable`, `ActionPanel`, `GameClient` — 실제 게임 UI |
| `dealer/[id]/` | 딜러 단말 |
| `dashboard/[id]/` | 전광판. 폴링과 블라인드 타이머가 들어 있다 |
| `login/`, `register/` | 미들웨어가 `/login`으로 보내므로 셸이 돌려면 있어야 한다 |

| 지운다 | 대체 |
|---|---|
| `tournaments/` | 계획 2가 새로 쓴다 |
| `user/` | 계획 2가 새로 쓴다 |
| `store/` | 계획 5가 새로 쓴다 |
| `playsync/page.tsx` | 개발용 목록 화면 |
| `types/`, `component/` | 위 화면들에서만 쓰던 것이면 함께 지운다 (Step 7에서 확인) |

**Files:**
- Create: `frontend/src/app/(player)/layout.tsx`
- Create: `frontend/src/app/(player)/layout.test.tsx`
- Create: `frontend/src/app/(console)/layout.tsx`
- Create: `frontend/src/app/(console)/layout.test.tsx`
- Create: `frontend/src/app/(terminal)/layout.tsx`
- Create: `frontend/src/app/(terminal)/layout.test.tsx`
- Move: 아래 표대로

| 기존 | 이동 후 |
|---|---|
| `app/dashboard/[id]/` | `app/(console)/stores/[storeId]/tournaments/[tournamentId]/display/` |
| `app/playsync/[tableId]/` | `app/(terminal)/table/[tableId]/` |
| `app/dealer/[id]/` | `app/(terminal)/dealer/[id]/` |
| `app/login/`, `app/register/` | 그대로 둔다 (그룹 밖 최상위) |

- Delete: `app/tournaments/`, `app/user/`, `app/store/`, `app/playsync/page.tsx`

**Interfaces:**
- Consumes: Task 1의 테스트 기반
- Produces: 세 개의 레이아웃 컴포넌트. 후속 계획의 페이지들이 이 아래에 들어간다.

- [ ] **Step 1: 실패하는 레이아웃 테스트 작성**

`frontend/src/app/(console)/layout.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import ConsoleLayout from './layout';

describe('콘솔 레이아웃', () => {
  it('사이드바와 본문을 함께 그린다', () => {
    render(<ConsoleLayout>{<p>본문</p>}</ConsoleLayout>);

    expect(screen.getByRole('navigation')).toBeInTheDocument();
    expect(screen.getByText('본문')).toBeInTheDocument();
  });
});
```

`frontend/src/app/(player)/layout.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import PlayerLayout from './layout';

describe('플레이어 레이아웃', () => {
  it('하단 탭과 본문을 함께 그린다', () => {
    render(<PlayerLayout>{<p>본문</p>}</PlayerLayout>);

    expect(screen.getByRole('navigation')).toBeInTheDocument();
    expect(screen.getByText('본문')).toBeInTheDocument();
  });
});
```

`frontend/src/app/(terminal)/layout.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import TerminalLayout from './layout';

describe('단말 레이아웃', () => {
  it('본문만 그리고 크롬을 두지 않는다', () => {
    render(<TerminalLayout>{<p>본문</p>}</TerminalLayout>);

    expect(screen.getByText('본문')).toBeInTheDocument();
    expect(screen.queryByRole('navigation')).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

실행: `npm run test -w frontend`

예상: 세 파일 모두 `Failed to resolve import "./layout"` 로 실패

- [ ] **Step 3: 콘솔 레이아웃 구현**

`frontend/src/app/(console)/layout.tsx`:

```tsx
import Link from 'next/link';

export default function ConsoleLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      <nav aria-label="콘솔 메뉴">
        <ul>
          <li>
            <Link href="/stores">상점</Link>
          </li>
          <li>
            <Link href="/admin">플랫폼 관리</Link>
          </li>
        </ul>
      </nav>
      <main>{children}</main>
    </div>
  );
}
```

- [ ] **Step 4: 플레이어 레이아웃 구현**

`frontend/src/app/(player)/layout.tsx`:

```tsx
import Link from 'next/link';

export default function PlayerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      <main>{children}</main>
      <nav aria-label="주요 메뉴">
        <Link href="/tournaments">대회</Link>
        <Link href="/me">내 정보</Link>
      </nav>
    </div>
  );
}
```

- [ ] **Step 5: 단말 레이아웃 구현**

`frontend/src/app/(terminal)/layout.tsx`:

```tsx
export default function TerminalLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // 단말은 좌석과 딜러석에 고정된 전체화면이다. 네비게이션을 두지 않는다.
  return <div>{children}</div>;
}
```

- [ ] **Step 6: 테스트 통과 확인**

실행: `npm run test -w frontend`

예상: `23 passed`

- [ ] **Step 7: 건질 페이지 이동, 나머지 삭제**

```bash
cd frontend/src/app
mkdir -p "(player)" "(console)/stores/[storeId]/tournaments/[tournamentId]" "(terminal)/table" "(terminal)/dealer"

git mv dashboard/\[id\] "(console)/stores/[storeId]/tournaments/[tournamentId]/display"
rmdir dashboard
git mv playsync/\[tableId\] "(terminal)/table/[tableId]"
git mv dealer/\[id\] "(terminal)/dealer/[id]"
rmdir dealer

git rm -r tournaments user store playsync/page.tsx
rmdir playsync
cd ../../..
```

`(player)` 그룹은 레이아웃만 두고 페이지 없이 비워 둔다. 계획 2가 채운다.

- [ ] **Step 8: 남은 참조 확인**

실행: `grep -rn "app/types\|@/app/types\|component/ActionTimer" frontend/src`

삭제한 화면에서만 쓰던 파일이면 함께 지운다:

```bash
git rm -r frontend/src/app/types frontend/src/component
```

살아 있는 화면이 아직 참조하면 지우지 않는다. 출력이 비어 있을 때만 지운다.

- [ ] **Step 9: 타입 체크와 빌드 확인**

실행: `npm run typecheck -w frontend`

예상: 에러 0건. 지운 화면을 참조하는 import가 남아 있으면 여기서 잡힌다

실행: `npm run build -w frontend`

예상: 빌드 성공. 라우트 목록에 `/login`, `/register`, `/table/[tableId]`, `/dealer/[id]`, `/stores/[storeId]/tournaments/[tournamentId]/display`가 보인다

- [ ] **Step 10: 커밋**

```bash
git add -A frontend/src
git commit -m "refactor: 라우트를 면 단위 그룹으로 재편하고 MVP 화면 정리"
```

---

### Task 5: 레거시 정리

**Files:**
- Delete: `frontend/src/app/lib/axios-instance.ts`
- Delete: `frontend/src/app/page.tsx`
- Create: `frontend/src/app/page.tsx` (리다이렉트로 교체)
- Create: `frontend/src/app/page.test.tsx`

**Interfaces:**
- Consumes: Task 4의 라우트 구조
- Produces: 없음

`axios-instance.ts`는 쿠키의 `auth_id`를 읽어 `x-temp-user-id` 헤더로 보낸다. 백엔드에 이 헤더를 읽는 코드는 없다. 죽은 코드이면서, 살아나면 클라이언트가 아무 유저나 사칭할 수 있는 형태다. 인증 경로를 JWT 쿠키 하나로 통일한다.

랜딩 페이지는 만들지 않는다. 루트는 로그인으로 보낸다.

- [ ] **Step 1: 사용처 확인**

실행: `grep -rn "axios-instance\|gameApi" frontend/src`

예상: `frontend/src/app/lib/axios-instance.ts` 한 줄만 나온다. 다른 파일이 나오면 그 파일을 `apiFetch`로 먼저 바꾼 뒤 진행한다

- [ ] **Step 2: 실패하는 테스트 작성**

`frontend/src/app/page.test.tsx`:

```tsx
import { describe, it, expect, vi } from 'vitest';

const redirect = vi.fn();
vi.mock('next/navigation', () => ({ redirect }));

describe('루트 페이지', () => {
  it('로그인으로 보낸다', async () => {
    const { default: RootPage } = await import('./page');
    RootPage();

    expect(redirect).toHaveBeenCalledWith('/login');
  });
});
```

- [ ] **Step 3: 테스트가 실패하는지 확인**

실행: `npm run test -w frontend`

예상: `redirect` 가 호출되지 않아 실패

- [ ] **Step 4: 루트 페이지 교체**

`frontend/src/app/page.tsx` 전체를 다음으로 교체:

```tsx
import { redirect } from 'next/navigation';

// 랜딩 페이지는 만들지 않는다. 진입점은 로그인이다.
export default function RootPage() {
  redirect('/login');
}
```

- [ ] **Step 5: 레거시 파일 삭제**

```bash
git rm frontend/src/app/lib/axios-instance.ts
```

- [ ] **Step 6: 테스트와 타입 체크 확인**

실행: `npm run test -w frontend`

예상: `24 passed`

실행: `npm run typecheck -w frontend`

예상: 에러 0건

- [ ] **Step 7: 커밋**

```bash
git add -A frontend/src
git commit -m "refactor: 사칭 가능한 임시 인증 헤더와 개발용 랜딩 제거"
```

---

## 완료 조건

- `npm run test` — contract · backend · frontend 전부 통과
- `npm run typecheck` — 에러 0건
- `npm run build` — 성공
- `frontend/src/app` 아래에 `(player)` · `(console)` · `(terminal)` 세 그룹이 있고 각각 레이아웃을 갖는다
- 목업 핸들러가 없는 요청은 테스트에서 에러가 난다

## 이 계획에 없는 것

후속 계획으로 넘어간다.

| 계획 | 범위 |
|---|---|
| 2 | 모바일 플레이어 — 지갑, 대회 찾기, 테이블 선택 등록, OTP 표시 |
| 3 | 좌석 태블릿 — OTP 진입, 게임 화면 |
| 4 | 딜러 단말 — 핸드 진행, 승자 입력, 좌석 정리 |
| 5 | 상점 콘솔 — 대시보드, 대회 운영, 블라인드 구조, 전광판 |
| 6 | 플랫폼 어드민 — 상점 목록, 상점 상세, 회원 |
