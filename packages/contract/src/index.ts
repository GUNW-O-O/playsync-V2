/**
 * 백엔드와 프론트엔드가 공유하는 계약.
 *
 * 여기 있는 것은 **경계를 넘는 것뿐**이다 — WS 이벤트 페이로드, HTTP DTO.
 * Prisma 모델이나 백엔드 내부 함수 인자는 넣지 않는다.
 *
 * 인바운드(클라 → 서버)는 `.strict()`라 모르는 키를 거부한다.
 * 아웃바운드(서버 → 클라)는 zod 기본 스트립이라, 백엔드에 필드를 추가해도
 * 스키마에 없으면 조용히 제거되어 자동으로 새지 않는다.
 */
export * from "./action";
export * from "./dealer-action";
