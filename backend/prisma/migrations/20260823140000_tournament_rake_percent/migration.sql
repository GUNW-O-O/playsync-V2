-- 상점 몫(레이크)을 대회마다 정한다.
--
-- 참가자가 따로 내는 수수료가 아니다. 참가비는 그대로 걷히고, 대회를 닫을 때
-- totalBuyinAmount에 한 번 곱해 뗀다. 건당 계산하지 않는 이유는 내림이 한
-- 번뿐이어야 하기 때문이다 — 999원짜리 대회에 100명이 들어와도 건당 내림이면
-- 상점 몫이 통째로 사라진다.
--
-- 기본값 0은 「레이크 없음」이다. 이미 있는 대회는 전부 그 뜻으로 남고,
-- 프라이즈풀 계산이 지금과 똑같이 돈다.
ALTER TABLE "Tournament" ADD COLUMN "rakePercent" INTEGER NOT NULL DEFAULT 0;
