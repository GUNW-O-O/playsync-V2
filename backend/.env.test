# 통합 테스트 전용 환경변수. docker-compose.test.yml과 짝을 이룬다.
# 비밀값이 아니므로 커밋한다 — 테스트 컨테이너는 로컬에만 뜨고 데이터가 남지 않는다.

DATABASE_URL=postgresql://test:test@localhost:5433/playsync_test
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=test
JWT_SECRET=test-only-not-a-real-secret
